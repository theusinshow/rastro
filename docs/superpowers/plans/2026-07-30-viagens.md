# Viagens — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Planejar, salvar e concluir um passeio de moto de um dia, de modo que a viagem concluída gere visitas registradas e o mapa se preencha sozinho.

**Architecture:** Motor puro em `src/domain/itinerary.ts` — seleção de paradas por interesse, ordenação por 2-opt. A malha viária real vem do OpenRouteService através de uma interface de uma função só que devolve `null` como única forma de falha, e o modo estimado é o `else` desse `if`. Persistência em repositório que não recebe `userId`, porque a RLS filtra. Concluir uma viagem é uma função Postgres `security invoker`, porque escreve em duas tabelas e precisa ser atômico.

**Tech Stack:** Next.js 16 (App Router, Server Actions), React 19, TypeScript strict com `noUncheckedIndexedAccess`, Tailwind CSS v4 CSS-first, MapLibre GL v5, Supabase (`@supabase/supabase-js`, `@supabase/ssr`), Vitest, Playwright para verificação visual.

**Spec:** `docs/superpowers/specs/2026-07-29-viagens-design.md` (commit `1c6bd90`)

## Global Constraints

Estas valem para **toda** tarefa. Não são repetidas em cada uma.

- **`CHANGELOG.md` em todo commit.** Sem exceção para "só documentação" ou "só um ajuste". Categoria em PT-BR: `Adicionado`, `Alterado`, `Corrigido`, `Removido`. Escreva para quem vai ler em seis meses, do ponto de vista de quem usa o produto — não o nome do arquivo editado.
- **Ordem obrigatória do fluxo:** alterar → `npm run lint` → `npm run typecheck` → escrever no `CHANGELOG.md` → `git add` incluindo o CHANGELOG → `git commit`.
- **`npm run lint` roda com `--max-warnings=0`.** Warning é erro.
- **Nenhum `any` novo, nenhum `@ts-ignore` novo, nenhum `console.log` esquecido.**
- **Camadas, verificáveis lendo imports:** `src/domain/` não importa nada do projeto além de `domain` — nem React, nem Next, nem componentes. `src/lib/routing/` e `src/lib/map/` importam só `domain`. `src/lib/data/` importa `domain` e `lib/supabase`. `src/app/actions/` importa `domain` e `lib`. `src/components/` importa `domain` e `lib`.
- **Repositórios nunca recebem `userId` para filtrar, e nunca escrevem `.eq('user_id', …)`.** A RLS filtra por `auth.uid()`. Ver ADR 0008. (`userId` entra só quando é *valor gravado*, como `created_by`.)
- **Server Actions são a fronteira de escrita.** Validam com funções puras do domínio, chamam repositórios, revalidam. **Nenhuma regra de negócio dentro delas.**
- **Componente visual não contém lógica de negócio.** Não calcula distância, não filtra, não ordena, não decide elegibilidade.
- **Direção visual (ADR 0009, 0010, 0012) — proibido:** card com conteúdo primário, gradiente, glassmorphism decorativo, ícone sem função, dashboard de widgets, sombra difusa, grade de botões de peso idêntico, **toast, tooltip só de hover, skeleton, modal**. Erro aparece no painel via `InlineMessage`.
- **Obrigatório:** separação por hairline de 1px, raio escolhido na escala (`--radius-sm` 8 / `md` 14 / `lg` 20 / `xl` 28 / `2xl` 36), todo dado numérico em `.instrument-value` (mono, `tabular-nums`), piso de corpo de 17px, acento âmbar.
- **Glifo é proibido fora da navegação principal** (ADR 0011). Não invente ícone para reordenar, remover ou confirmar.
- **O mapa não lê variável CSS.** MapLibre desenha em WebGL: cores em `src/lib/map/` são hex literal espelhando a paleta à mão. Paleta vigente (ADR 0012): `void #11100f`, `base #181716`, `ink #eae3d8`, `accent #e0a02e`, `visited #8fa36a`, `unvisited #93887a`.
- **Textos de interface em PT-BR; código em inglês.**
- **Comentário explica o *porquê*, nunca o *quê*.** Sem comentário que repete o código.
- **Dados de `src/mocks/` não são informação verificada** e não podem ser apresentados como tal.
- **Nenhuma dependência nova sem justificativa.** Este projeto prefere ~80 linhas próprias a um wrapper. Já recusou `react-map-gl`, bibliotecas de estado global e de data.
- **`lint`, `typecheck` e os testes não desenham o mapa.** Toda tarefa que toca camada de mapa exige captura de tela antes de ser declarada pronta. Isto já custou 14 pins invisíveis neste repositório.

---

## File Structure

| Arquivo | Responsabilidade |
|---|---|
| `src/domain/itinerary.ts` | Pontuação de interesse, ordenação, montagem e recusa. Puro. |
| `src/domain/itinerary.test.ts` | Testes do acima. |
| `src/lib/routing/routing-client.ts` | Interface `RoutingClient` e tipo `RoutedLine`. |
| `src/lib/routing/open-route-service.ts` | Única implementação. Traduz ORS → `RoutedLine`. |
| `src/lib/routing/index.ts` | `getRoutingClient()` — devolve o cliente ou `null` sem chave. |
| `src/lib/data/trip-repository.ts` | Contrato de leitura e escrita de viagens. |
| `src/lib/data/supabase/trip-row.ts` | Tradução linha ↔ domínio, e a conversão de data. |
| `src/lib/data/supabase/trip-row.test.ts` | Testes do acima, incluindo o caso das 23h. |
| `src/lib/data/supabase/supabase-trip-repository.ts` | Adapter. |
| `supabase/migrations/0003_complete_trip.sql` | Função `complete_trip`, `security invoker`. |
| `src/app/actions/trip-actions.ts` | Criar, salvar paradas, concluir, apagar. |
| `src/lib/map/trip-route-layers.ts` | As duas camadas de linha e o GeoJSON do traçado. |
| `src/components/map/TripRouteLayer.tsx` | Monta e desmonta as camadas no mapa. |
| `src/components/trips/TripList.tsx` | Lista de viagens. |
| `src/components/trips/TripProposalForm.tsx` | Formulário de proposta. |
| `src/components/trips/TripItineraryView.tsx` | Roteiro, paradas, ações. |
| `src/components/trips/TripCompletionForm.tsx` | Confirmação de paradas. |
| `src/domain/google-maps-link.ts` | Monta a URL de waypoints. Puro. |
| `docs/decisions/0013-openrouteservice-para-tracado.md` | ADR da dependência. |

---

# Fase 1 — Domínio

### Task 1: Pontuação de interesse

**Files:**
- Create: `src/domain/itinerary.ts`
- Create: `src/domain/itinerary.test.ts`
- Modify: `src/domain/discovery.ts` (adicionar `MINUTES_PER_STOP`)

**Interfaces:**
- Consumes: `ExplorePlace` de `src/domain/place.ts`; `TIME_BUDGET_MINUTES` de `src/domain/discovery.ts`.
- Produces: `SCORE_WANTS_TO_VISIT`, `SCORE_UNVISITED`, `SCORE_VISITED`, `SCORE_FAVORITE_BONUS`, `SCORE_STALENESS_MAX`, `STALENESS_SATURATION_MONTHS`, `scorePlace(place: ExplorePlace, today: string): number`, `MINUTES_PER_STOP`.

- [ ] **Step 1: Escrever o teste que falha**

Crie `src/domain/itinerary.test.ts`. A fábrica `place()` é duplicada de `discovery.test.ts` de propósito: o `CLAUDE.md` prefere duplicar duas vezes a abstrair cedo e errado.

```ts
import { describe, expect, it } from 'vitest'
import {
  SCORE_STALENESS_MAX,
  SCORE_UNVISITED,
  SCORE_VISITED,
  SCORE_WANTS_TO_VISIT,
  scorePlace,
} from './itinerary'
import type { ExplorePlace } from './place'

const TODAY = '2026-07-30'

function place(overrides: Partial<ExplorePlace>): ExplorePlace {
  return {
    id: 'x',
    slug: 'x',
    name: 'X',
    description: '',
    latitude: -27.6455,
    longitude: -48.67,
    municipality: 'Palhoça',
    stateCode: 'SC',
    category: 'serra',
    tags: [],
    coverImageUrl: null,
    source: 'mock',
    visitStatus: 'nao-visitado',
    isFavorite: false,
    visits: [],
    isOwn: false,
    photoCount: 0,
    lastVisitedAt: null,
    ...overrides,
  }
}

describe('scorePlace', () => {
  it('põe quero-conhecer acima de não visitado, e não visitado acima de visitado', () => {
    const wanted = scorePlace(place({ visitStatus: 'quero-conhecer' }), TODAY)
    const unvisited = scorePlace(place({ visitStatus: 'nao-visitado' }), TODAY)
    const visited = scorePlace(
      place({ visitStatus: 'visitado', lastVisitedAt: TODAY }),
      TODAY,
    )

    expect(wanted).toBeGreaterThan(unvisited)
    expect(unvisited).toBeGreaterThan(visited)
  })

  it('dá bônus a favorito', () => {
    const plain = scorePlace(place({}), TODAY)
    const favorite = scorePlace(place({ isFavorite: true }), TODAY)

    expect(favorite).toBeGreaterThan(plain)
  })

  it('vale mais o lugar visitado há muito tempo que o visitado agora', () => {
    const recent = scorePlace(
      place({ visitStatus: 'visitado', lastVisitedAt: '2026-07-01' }),
      TODAY,
    )
    const old = scorePlace(
      place({ visitStatus: 'visitado', lastVisitedAt: '2020-01-01' }),
      TODAY,
    )

    expect(old).toBeGreaterThan(recent)
  })

  it('satura o bônus de recência, para lugar antigo não vencer quero-conhecer', () => {
    const ancient = scorePlace(
      place({ visitStatus: 'visitado', lastVisitedAt: '1990-01-01' }),
      TODAY,
    )

    expect(ancient).toBe(SCORE_VISITED + SCORE_STALENESS_MAX)
    expect(ancient).toBeLessThan(SCORE_WANTS_TO_VISIT)
  })

  it('não dá bônus de recência a quem nunca foi visitado', () => {
    expect(scorePlace(place({ visitStatus: 'nao-visitado' }), TODAY)).toBe(
      SCORE_UNVISITED,
    )
  })
})
```

- [ ] **Step 2: Rodar o teste para confirmar que falha**

Run: `npx vitest run src/domain/itinerary.test.ts`
Expected: FAIL — `Failed to resolve import "./itinerary"`

- [ ] **Step 3: Adicionar `MINUTES_PER_STOP` ao `discovery.ts`**

No fim de `src/domain/discovery.ts`, junto das outras constantes de tempo (após `RIDING_TIME_RATIO`):

```ts
/**
 * Tempo parado por parada: foto, café, esticar as pernas, combustível.
 *
 * Existe porque o `RIDING_TIME_RATIO` acima NÃO generaliza. Ele foi calibrado
 * para um destino, reservando 25% do tempo para tudo o que não é pilotar — e
 * cinco paradas não custam a mesma proporção que uma. Para um destino em quatro
 * horas o ratio reserva 60 minutos; este modelo reserva 30.
 *
 * A divergência é consciente e está registrada na spec: mudar o `/descobrir`
 * alteraria comportamento já calibrado sem dado que justifique. O gatilho para
 * unificar as duas réguas é esta própria funcionalidade passar a gravar viagens
 * com distância medida — então dá para calibrar com dado em vez de palpite.
 */
export const MINUTES_PER_STOP = 30
```

- [ ] **Step 4: Escrever a implementação mínima**

Crie `src/domain/itinerary.ts`:

```ts
import type { ExplorePlace } from './place'

/**
 * Pontuação de interesse de um lugar como parada de um roteiro.
 *
 * Esta é a informação que o Rastro tem e um otimizador genérico não: o que você
 * marcou como quero-conhecer, o que ainda não viu, e há quanto tempo não volta.
 * É por isso que a ORDEM das paradas pode ser delegada à geometria, mas a
 * ESCOLHA delas não.
 */
export const SCORE_WANTS_TO_VISIT = 100
export const SCORE_UNVISITED = 60
export const SCORE_VISITED = 20
export const SCORE_FAVORITE_BONUS = 25

/** Máximo que a idade da última visita soma a um lugar já visitado. */
export const SCORE_STALENESS_MAX = 30

/** A partir daqui a visita é "antiga" e o bônus para de crescer. */
export const STALENESS_SATURATION_MONTHS = 24

const DAYS_PER_MONTH = 30.44

/**
 * Meses inteiros entre duas datas civis `YYYY-MM-DD`.
 *
 * O parse é manual pelo mesmo motivo do `dates.ts`: `new Date('2026-07-26')` é
 * meia-noite UTC e, no fuso de Brasília, cai no dia anterior. Isto é conta de
 * calendário, e `Date` arrastaria fuso para dentro dela.
 */
function monthsBetween(fromIsoDate: string, toIsoDate: string): number {
  const from = Date.parse(`${fromIsoDate}T12:00:00Z`)
  const to = Date.parse(`${toIsoDate}T12:00:00Z`)
  if (Number.isNaN(from) || Number.isNaN(to)) return 0

  const days = (to - from) / 86_400_000
  return Math.max(0, days / DAYS_PER_MONTH)
}

/**
 * Voltar a um lugar tem valor, e voltar a um lugar que você não vê há dois anos
 * tem mais. Satura de propósito: sem teto, um lugar visitado em 1990 venceria
 * um quero-conhecer, e o roteiro deixaria de servir a quem quer conhecer coisa
 * nova.
 *
 * `today` é `YYYY-MM-DD` e entra por parâmetro, não de `new Date()` aqui: é o
 * que mantém a função pura e testável.
 */
export function scorePlace(place: ExplorePlace, today: string): number {
  let score = 0

  if (place.visitStatus === 'quero-conhecer') score += SCORE_WANTS_TO_VISIT
  else if (place.visitStatus === 'nao-visitado') score += SCORE_UNVISITED
  else {
    score += SCORE_VISITED
    if (place.lastVisitedAt) {
      const months = monthsBetween(place.lastVisitedAt, today)
      const ratio = Math.min(1, months / STALENESS_SATURATION_MONTHS)
      score += ratio * SCORE_STALENESS_MAX
    }
  }

  if (place.isFavorite) score += SCORE_FAVORITE_BONUS

  return score
}
```

- [ ] **Step 5: Rodar o teste para confirmar que passa**

Run: `npx vitest run src/domain/itinerary.test.ts`
Expected: PASS — 5 testes.

- [ ] **Step 6: Rodar lint e typecheck**

Run: `npm run lint && npm run typecheck`
Expected: ambos sem saída de erro.

- [ ] **Step 7: Escrever no CHANGELOG e commitar**

Em `CHANGELOG.md`, sob `## [Não lançado]` → `### Adicionado`:

```markdown
- Ao propor um roteiro, o Rastro passa a saber o que te interessa: lugar que você
  marcou como quero-conhecer vem antes do que ainda não viu, que vem antes de onde
  já esteve — e voltar a um lugar que você não vê há dois anos vale mais que
  voltar ao de mês passado
```

```bash
git add src/domain/itinerary.ts src/domain/itinerary.test.ts src/domain/discovery.ts CHANGELOG.md
git commit -m "feat: o roteiro sabe o que te interessa"
```

---

### Task 2: Ordenação e medição

**Files:**
- Modify: `src/domain/itinerary.ts`
- Modify: `src/domain/itinerary.test.ts`

**Interfaces:**
- Consumes: `scorePlace` (Task 1); `Coordinates` e `haversineKm` de `src/domain/geo.ts`; `estimateRoadKm` e `estimateRidingMinutes` de `src/domain/discovery.ts`.
- Produces: `ItineraryLeg { fromStopIndex: number; roadKm: number; minutes: number }`, `Itinerary { stops: ExplorePlace[]; legs: ItineraryLeg[]; totalRoadKm: number; totalRidingMinutes: number }`, `orderAndMeasure(origin: Coordinates, stops: readonly ExplorePlace[]): Itinerary`.

- [ ] **Step 1: Escrever o teste que falha**

Acrescente a `src/domain/itinerary.test.ts` (e adicione `orderAndMeasure` ao `import` do topo):

```ts
const ORIGIN = { latitude: -27.6455, longitude: -48.67 }

describe('orderAndMeasure', () => {
  it('devolve roteiro vazio para nenhuma parada', () => {
    const result = orderAndMeasure(ORIGIN, [])

    expect(result.stops).toEqual([])
    expect(result.legs).toEqual([])
    expect(result.totalRoadKm).toBe(0)
  })

  it('fecha o ciclo: a última perna volta para a origem', () => {
    const result = orderAndMeasure(ORIGIN, [
      place({ slug: 'a', latitude: -28, longitude: -49 }),
    ])

    // Uma parada: origem -> a, e a -> origem.
    expect(result.legs).toHaveLength(2)
    expect(result.legs[0]?.fromStopIndex).toBe(-1)
    expect(result.legs[1]?.fromStopIndex).toBe(0)
  })

  it('ordena para não cruzar: o caminho ruim é reordenado num mais curto', () => {
    // Quatro pontos num quadrado, entregues na ordem que se cruza (A, C, B, D).
    const square = [
      place({ slug: 'a', latitude: -27.0, longitude: -49.0 }),
      place({ slug: 'c', latitude: -28.0, longitude: -48.0 }),
      place({ slug: 'b', latitude: -27.0, longitude: -48.0 }),
      place({ slug: 'd', latitude: -28.0, longitude: -49.0 }),
    ]

    const result = orderAndMeasure({ latitude: -27.5, longitude: -48.5 }, square)
    const order = result.stops.map((stop) => stop.slug).join('')

    // O perímetro do quadrado, em algum sentido — nunca a diagonal cruzada.
    expect(['abcd', 'adcb', 'bcda', 'badc', 'cdab', 'cbad', 'dabc', 'dcba'])
      .toContain(order)
  })

  it('soma os trechos no total', () => {
    const result = orderAndMeasure(ORIGIN, [
      place({ slug: 'a', latitude: -28, longitude: -49 }),
      place({ slug: 'b', latitude: -28.5, longitude: -49.5 }),
    ])

    const somaDosTrechos = result.legs.reduce((sum, leg) => sum + leg.roadKm, 0)
    expect(result.totalRoadKm).toBeCloseTo(somaDosTrechos, 6)
    expect(result.totalRoadKm).toBeGreaterThan(0)
  })
})
```

- [ ] **Step 2: Rodar o teste para confirmar que falha**

Run: `npx vitest run src/domain/itinerary.test.ts`
Expected: FAIL — `orderAndMeasure is not exported` / `is not a function`.

- [ ] **Step 3: Escrever a implementação**

Acrescente a `src/domain/itinerary.ts`, e complete os imports do topo:

```ts
import { estimateRidingMinutes, estimateRoadKm } from './discovery'
import { haversineKm, type Coordinates } from './geo'
```

```ts
export interface ItineraryLeg {
  /** Índice da parada de onde o trecho sai. `-1` é a origem. */
  fromStopIndex: number
  roadKm: number
  minutes: number
}

export interface Itinerary {
  /** Já na ordem de rodagem. */
  stops: ExplorePlace[]
  /** Inclui o trecho de volta à origem. */
  legs: ItineraryLeg[]
  totalRoadKm: number
  totalRidingMinutes: number
}

/** Quilômetros rodoviários estimados de um ciclo fechado origem → pontos → origem. */
function cycleRoadKm(
  origin: Coordinates,
  stops: readonly Coordinates[],
): number {
  let total = 0
  let previous: Coordinates = origin

  for (const stop of stops) {
    total += estimateRoadKm(haversineKm(previous, stop))
    previous = stop
  }

  return total + estimateRoadKm(haversineKm(previous, origin))
}

/**
 * Vizinho-mais-próximo a partir da origem.
 *
 * É o ponto de partida do 2-opt, não a resposta: sozinho ele produz um caminho
 * que às vezes cruza consigo mesmo.
 */
function nearestNeighbourOrder(
  origin: Coordinates,
  stops: readonly ExplorePlace[],
): ExplorePlace[] {
  const remaining = [...stops]
  const ordered: ExplorePlace[] = []
  let current: Coordinates = origin

  while (remaining.length > 0) {
    let bestIndex = 0
    let bestKm = Infinity

    for (let i = 0; i < remaining.length; i += 1) {
      const candidate = remaining[i]
      if (!candidate) continue
      const km = haversineKm(current, candidate)
      if (km < bestKm) {
        bestKm = km
        bestIndex = i
      }
    }

    const [chosen] = remaining.splice(bestIndex, 1)
    if (!chosen) break
    ordered.push(chosen)
    current = chosen
  }

  return ordered
}

/**
 * 2-opt sobre o ciclo fechado: inverte trechos enquanto isso encurtar o total.
 *
 * Com algumas dezenas de paradas isto roda em microssegundos, e é o que desfaz
 * os cruzamentos que o vizinho-mais-próximo deixa. Não busca o ótimo global —
 * busca um caminho que não pareça errado a olho no mapa, que é o que importa.
 */
function twoOptImprove(
  origin: Coordinates,
  stops: readonly ExplorePlace[],
): ExplorePlace[] {
  let best = [...stops]
  let bestKm = cycleRoadKm(origin, best)
  let improved = true

  while (improved) {
    improved = false

    for (let i = 0; i < best.length - 1; i += 1) {
      for (let j = i + 1; j < best.length; j += 1) {
        const candidate = [
          ...best.slice(0, i),
          ...best.slice(i, j + 1).reverse(),
          ...best.slice(j + 1),
        ]
        const km = cycleRoadKm(origin, candidate)
        if (km < bestKm - 1e-9) {
          best = candidate
          bestKm = km
          improved = true
        }
      }
    }
  }

  return best
}

/**
 * Ordena um conjunto de paradas já escolhido e mede os trechos.
 *
 * É o que o modo "montar no dedo" usa: nenhuma seleção, só geometria. A ordem
 * nunca é a que o usuário informou — ordenar é trabalho de máquina, e o 2-opt
 * faz melhor que a mão.
 */
export function orderAndMeasure(
  origin: Coordinates,
  stops: readonly ExplorePlace[],
): Itinerary {
  if (stops.length === 0) {
    return { stops: [], legs: [], totalRoadKm: 0, totalRidingMinutes: 0 }
  }

  const ordered = twoOptImprove(origin, nearestNeighbourOrder(origin, stops))

  const legs: ItineraryLeg[] = []
  let previous: Coordinates = origin

  ordered.forEach((stop, index) => {
    const roadKm = estimateRoadKm(haversineKm(previous, stop))
    legs.push({
      fromStopIndex: index - 1,
      roadKm,
      minutes: estimateRidingMinutes(roadKm),
    })
    previous = stop
  })

  const returnKm = estimateRoadKm(haversineKm(previous, origin))
  legs.push({
    fromStopIndex: ordered.length - 1,
    roadKm: returnKm,
    minutes: estimateRidingMinutes(returnKm),
  })

  const totalRoadKm = legs.reduce((sum, leg) => sum + leg.roadKm, 0)

  return {
    stops: ordered,
    legs,
    totalRoadKm,
    totalRidingMinutes: estimateRidingMinutes(totalRoadKm),
  }
}
```

- [ ] **Step 4: Rodar o teste para confirmar que passa**

Run: `npx vitest run src/domain/itinerary.test.ts`
Expected: PASS — 9 testes.

- [ ] **Step 5: Rodar lint e typecheck**

Run: `npm run lint && npm run typecheck`
Expected: ambos limpos.

- [ ] **Step 6: Escrever no CHANGELOG e commitar**

Sob `### Adicionado`:

```markdown
- A ordem das paradas de um roteiro é calculada, não arrastada: o Rastro
  reorganiza o caminho para ele não cruzar consigo mesmo
```

```bash
git add src/domain/itinerary.ts src/domain/itinerary.test.ts CHANGELOG.md
git commit -m "feat: a ordem das paradas e calculada, nao arrastada"
```

---

### Task 3: Montagem do roteiro e as três recusas

**Files:**
- Modify: `src/domain/itinerary.ts`
- Modify: `src/domain/itinerary.test.ts`

**Interfaces:**
- Consumes: `scorePlace` (Task 1), `orderAndMeasure` (Task 2), `TIME_BUDGET_MINUTES` e `MINUTES_PER_STOP` de `discovery.ts`, `PlaceCategory` de `place.ts`, `TimeBudget` de `discovery.ts`.
- Produces: `ItineraryRequest`, `ItineraryRefusal` (`'no-candidates' | 'budget-too-small' | 'anchor-does-not-fit'`), `ItineraryOutcome`, `buildItinerary(places, request, today): ItineraryOutcome`, `isRefusal(outcome): outcome is { refusal: ItineraryRefusal }`.

- [ ] **Step 1: Escrever o teste que falha**

Acrescente a `src/domain/itinerary.test.ts` (e complete o `import`):

```ts
import { buildItinerary, isRefusal } from './itinerary'
import type { ItineraryRequest } from './itinerary'

function request(overrides: Partial<ItineraryRequest> = {}): ItineraryRequest {
  return {
    origin: ORIGIN,
    timeBudget: 'dia-inteiro',
    maxDistanceKm: 300,
    categories: [],
    anchorPlaceId: null,
    maxStops: 4,
    ...overrides,
  }
}

describe('buildItinerary', () => {
  it('recusa com no-candidates quando nada passa pelos filtros', () => {
    const outcome = buildItinerary(
      [place({ slug: 'a', category: 'praia' })],
      request({ categories: ['cachoeira'] }),
      TODAY,
    )

    expect(isRefusal(outcome)).toBe(true)
    if (isRefusal(outcome)) expect(outcome.refusal).toBe('no-candidates')
  })

  it('recusa com budget-too-small quando passa pelo filtro mas nada cabe no tempo', () => {
    const outcome = buildItinerary(
      [place({ slug: 'longe', latitude: -26.0, longitude: -48.0 })],
      request({ timeBudget: '2h', maxDistanceKm: 1000 }),
      TODAY,
    )

    expect(isRefusal(outcome)).toBe(true)
    if (isRefusal(outcome)) expect(outcome.refusal).toBe('budget-too-small')
  })

  it('recusa com anchor-does-not-fit quando só a âncora já estoura', () => {
    const outcome = buildItinerary(
      [
        place({ id: 'anchor', slug: 'anchor', latitude: -26.0, longitude: -48.0 }),
        place({ id: 'perto', slug: 'perto', latitude: -27.65, longitude: -48.68 }),
      ],
      request({ timeBudget: '2h', maxDistanceKm: 1000, anchorPlaceId: 'anchor' }),
      TODAY,
    )

    expect(isRefusal(outcome)).toBe(true)
    if (isRefusal(outcome)) expect(outcome.refusal).toBe('anchor-does-not-fit')
  })

  it('nunca descarta a âncora, mesmo quando ela pontua menos', () => {
    const outcome = buildItinerary(
      [
        place({
          id: 'anchor',
          slug: 'anchor',
          visitStatus: 'visitado',
          lastVisitedAt: TODAY,
          latitude: -27.9,
          longitude: -49.0,
        }),
        place({ id: 'q1', slug: 'q1', visitStatus: 'quero-conhecer', latitude: -27.7, longitude: -48.8 }),
        place({ id: 'q2', slug: 'q2', visitStatus: 'quero-conhecer', latitude: -27.8, longitude: -48.9 }),
      ],
      request({ timeBudget: '4h', maxStops: 2, anchorPlaceId: 'anchor' }),
      TODAY,
    )

    expect(isRefusal(outcome)).toBe(false)
    if (!isRefusal(outcome)) {
      expect(outcome.stops.map((s) => s.id)).toContain('anchor')
    }
  })

  it('respeita maxStops', () => {
    const many = Array.from({ length: 8 }, (_, i) =>
      place({
        id: `p${i}`,
        slug: `p${i}`,
        latitude: -27.65 - i * 0.02,
        longitude: -48.68 - i * 0.02,
      }),
    )

    const outcome = buildItinerary(many, request({ maxStops: 3 }), TODAY)

    expect(isRefusal(outcome)).toBe(false)
    if (!isRefusal(outcome)) expect(outcome.stops.length).toBeLessThanOrEqual(3)
  })

  it('cabe no orçamento, descontando o tempo parado em cada parada', () => {
    const many = Array.from({ length: 6 }, (_, i) =>
      place({
        id: `p${i}`,
        slug: `p${i}`,
        latitude: -27.7 - i * 0.15,
        longitude: -48.8 - i * 0.15,
      }),
    )

    const outcome = buildItinerary(many, request({ timeBudget: '4h', maxStops: 5 }), TODAY)

    expect(isRefusal(outcome)).toBe(false)
    if (!isRefusal(outcome)) {
      const parado = outcome.stops.length * MINUTES_PER_STOP
      expect(outcome.totalRidingMinutes).toBeLessThanOrEqual(240 - parado)
    }
  })
})
```

Complete também o `import` de `./discovery` no teste:

```ts
import { MINUTES_PER_STOP } from './discovery'
```

- [ ] **Step 2: Rodar o teste para confirmar que falha**

Run: `npx vitest run src/domain/itinerary.test.ts`
Expected: FAIL — `buildItinerary is not a function`.

- [ ] **Step 3: Escrever a implementação**

Acrescente a `src/domain/itinerary.ts`, completando os imports:

```ts
import {
  MINUTES_PER_STOP,
  TIME_BUDGET_MINUTES,
  estimateRidingMinutes,
  estimateRoadKm,
  type TimeBudget,
} from './discovery'
import type { ExplorePlace, PlaceCategory } from './place'
```

```ts
export interface ItineraryRequest {
  origin: Coordinates
  timeBudget: TimeBudget
  /** Limite de distância rodoviária estimada, só de ida. */
  maxDistanceKm: number
  /** Vazio significa qualquer categoria. */
  categories: PlaceCategory[]
  /** Parada obrigatória, quando o usuário já sabe o destino. */
  anchorPlaceId: string | null
  maxStops: number
}

/**
 * As três recusas são distintas e rendem mensagens diferentes.
 *
 * Confundi-las devolveria ao usuário o trabalho de descobrir o que deu errado —
 * e sugerir "aumente o raio" quando o problema é tempo pioraria a situação.
 */
export type ItineraryRefusal =
  /** Nada passou pelos filtros de categoria e raio. Nem chegou a medir tempo. */
  | 'no-candidates'
  /** Passou pelos filtros, mas nem a parada mais próxima cabe no tempo. */
  | 'budget-too-small'
  /** Só a âncora, sozinha, já estoura o orçamento. */
  | 'anchor-does-not-fit'

export type ItineraryOutcome = Itinerary | { refusal: ItineraryRefusal }

export function isRefusal(
  outcome: ItineraryOutcome,
): outcome is { refusal: ItineraryRefusal } {
  return 'refusal' in outcome
}

/** Minutos de pilotagem disponíveis, já descontado o tempo parado. */
function ridingBudgetMinutes(budget: TimeBudget, stopCount: number): number {
  return TIME_BUDGET_MINUTES[budget] - stopCount * MINUTES_PER_STOP
}

function fits(itinerary: Itinerary, budget: TimeBudget): boolean {
  return (
    itinerary.totalRidingMinutes <=
    ridingBudgetMinutes(budget, itinerary.stops.length)
  )
}

/**
 * Monta o roteiro: seleciona por interesse, ordena por geometria, e poda até
 * caber.
 *
 * A poda descarta sempre a parada de MENOR pontuação e reordena — nunca a
 * âncora, que é o destino que o usuário já escolheu. Se só ela não cabe, a
 * recusa diz isso em vez de devolver uma lista vazia.
 */
export function buildItinerary(
  places: readonly ExplorePlace[],
  request: ItineraryRequest,
  today: string,
): ItineraryOutcome {
  const anchor = request.anchorPlaceId
    ? places.find((place) => place.id === request.anchorPlaceId)
    : undefined

  const candidates = places.filter((place) => {
    if (place.id === anchor?.id) return false
    if (
      request.categories.length > 0 &&
      !request.categories.includes(place.category)
    ) {
      return false
    }
    return (
      estimateRoadKm(haversineKm(request.origin, place)) <= request.maxDistanceKm
    )
  })

  if (!anchor && candidates.length === 0) return { refusal: 'no-candidates' }

  if (anchor) {
    const alone = orderAndMeasure(request.origin, [anchor])
    if (!fits(alone, request.timeBudget)) {
      return { refusal: 'anchor-does-not-fit' }
    }
  }

  const ranked = [...candidates].sort(
    (a, b) => scorePlace(b, today) - scorePlace(a, today),
  )

  const room = anchor ? request.maxStops - 1 : request.maxStops
  const selected = [
    ...(anchor ? [anchor] : []),
    ...ranked.slice(0, Math.max(0, room)),
  ]

  // Poda: enquanto não couber, sai a de menor pontuação que não seja a âncora.
  let working = selected
  while (working.length > 0) {
    const itinerary = orderAndMeasure(request.origin, working)
    if (fits(itinerary, request.timeBudget)) return itinerary

    const droppable = working.filter((place) => place.id !== anchor?.id)
    if (droppable.length === 0) return { refusal: 'anchor-does-not-fit' }

    let worst = droppable[0]
    if (!worst) break
    for (const place of droppable) {
      if (scorePlace(place, today) < scorePlace(worst, today)) worst = place
    }
    working = working.filter((place) => place.id !== worst.id)
  }

  return { refusal: 'budget-too-small' }
}
```

- [ ] **Step 4: Rodar o teste para confirmar que passa**

Run: `npx vitest run src/domain/itinerary.test.ts`
Expected: PASS — 15 testes.

- [ ] **Step 5: Rodar a suíte inteira, lint e typecheck**

Run: `npm test -- --run && npm run lint && npm run typecheck`
Expected: 136 testes passando (121 anteriores + 15 novos), lint e typecheck limpos.

Se algum teste anterior quebrar, é sinal de que `MINUTES_PER_STOP` ou o import em `discovery.ts` alterou comportamento existente — investigue antes de seguir, não ajuste o teste.

- [ ] **Step 6: Escrever no CHANGELOG e commitar**

Sob `### Adicionado`:

```markdown
- Quando um roteiro não cabe no tempo que você tem, o Rastro tira a parada menos
  interessante e recalcula, em vez de desistir. Se nem o destino que você escolheu
  cabe, ele diz isso — nunca devolve uma lista vazia sem explicação
```

```bash
git add src/domain/itinerary.ts src/domain/itinerary.test.ts CHANGELOG.md
git commit -m "feat: o roteiro poda o que nao cabe e explica quando nao da"
```

---

### Task 4: Link de waypoints para o Google Maps

**Files:**
- Create: `src/domain/google-maps-link.ts`
- Create: `src/domain/google-maps-link.test.ts`

**Interfaces:**
- Consumes: `Coordinates` de `src/domain/geo.ts`.
- Produces: `googleMapsRouteUrl(origin: Coordinates, stops: readonly Coordinates[]): string`.

- [ ] **Step 1: Escrever o teste que falha**

```ts
import { describe, expect, it } from 'vitest'
import { googleMapsRouteUrl } from './google-maps-link'

const ORIGIN = { latitude: -27.6455, longitude: -48.67 }

describe('googleMapsRouteUrl', () => {
  it('volta para a origem: ela é destino e ponto de partida', () => {
    const url = new URL(
      googleMapsRouteUrl(ORIGIN, [{ latitude: -28.1, longitude: -49.4 }]),
    )

    expect(url.searchParams.get('origin')).toBe('-27.6455,-48.67')
    expect(url.searchParams.get('destination')).toBe('-27.6455,-48.67')
    expect(url.searchParams.get('waypoints')).toBe('-28.1,-49.4')
  })

  it('separa múltiplas paradas por barra vertical', () => {
    const url = new URL(
      googleMapsRouteUrl(ORIGIN, [
        { latitude: -28.1, longitude: -49.4 },
        { latitude: -28.3, longitude: -49.6 },
      ]),
    )

    expect(url.searchParams.get('waypoints')).toBe('-28.1,-49.4|-28.3,-49.6')
  })

  it('sem paradas, não emite waypoints vazio', () => {
    const url = new URL(googleMapsRouteUrl(ORIGIN, []))

    expect(url.searchParams.has('waypoints')).toBe(false)
  })
})
```

- [ ] **Step 2: Rodar o teste para confirmar que falha**

Run: `npx vitest run src/domain/google-maps-link.test.ts`
Expected: FAIL — `Failed to resolve import "./google-maps-link"`.

- [ ] **Step 3: Escrever a implementação**

```ts
import type { Coordinates } from './geo'

/**
 * URL de rota no Google Maps com as paradas como waypoints.
 *
 * Esta função é a fronteira declarada do produto: o Rastro escolhe as paradas, o
 * Google traça o caminho entre elas. É por isso que não há motor de navegação
 * aqui — competir com o Google em curva-a-curva seria entregar versão pior do
 * que qualquer pessoa já tem no bolso.
 *
 * A origem aparece duas vezes de propósito: o passeio sai de casa e volta para
 * casa, então ela é partida e destino.
 */
export function googleMapsRouteUrl(
  origin: Coordinates,
  stops: readonly Coordinates[],
): string {
  const point = (c: Coordinates) => `${c.latitude},${c.longitude}`

  const url = new URL('https://www.google.com/maps/dir/')
  url.searchParams.set('api', '1')
  url.searchParams.set('travelmode', 'driving')
  url.searchParams.set('origin', point(origin))
  url.searchParams.set('destination', point(origin))

  if (stops.length > 0) {
    url.searchParams.set('waypoints', stops.map(point).join('|'))
  }

  return url.toString()
}
```

- [ ] **Step 4: Rodar o teste para confirmar que passa**

Run: `npx vitest run src/domain/google-maps-link.test.ts`
Expected: PASS — 3 testes.

- [ ] **Step 5: Rodar lint e typecheck**

Run: `npm run lint && npm run typecheck`
Expected: limpos.

- [ ] **Step 6: Escrever no CHANGELOG e commitar**

Sob `### Adicionado`:

```markdown
- Um roteiro montado no Rastro abre no Google Maps com as paradas já na ordem,
  saindo e voltando da sua origem
```

```bash
git add src/domain/google-maps-link.ts src/domain/google-maps-link.test.ts CHANGELOG.md
git commit -m "feat: o roteiro abre no Google Maps com as paradas na ordem"
```

---

# Fase 2 — A fronteira externa

### Task 5: Cliente de roteamento e o ADR

**Files:**
- Create: `src/lib/routing/routing-client.ts`
- Create: `src/lib/routing/open-route-service.ts`
- Create: `src/lib/routing/index.ts`
- Create: `docs/decisions/0013-openrouteservice-para-tracado.md`
- Modify: `.env.example` (se existir; se não, criar)

**Interfaces:**
- Consumes: `Coordinates` de `src/domain/geo.ts`.
- Produces: `RoutedLine { geometry: { type: 'LineString'; coordinates: [number, number][] }; roadKm: number; minutes: number }`, `RoutingClient { route(points): Promise<RoutedLine | null> }`, `createOpenRouteServiceClient(apiKey: string): RoutingClient`, `getRoutingClient(): RoutingClient | null`.

- [ ] **Step 1: Escrever a interface**

Crie `src/lib/routing/routing-client.ts`:

```ts
import type { Coordinates } from '@/domain/geo'

/** Traçado real da estrada entre uma sequência de pontos. */
export interface RoutedLine {
  /**
   * `LineString` em GeoJSON, coordenadas `[longitude, latitude]`.
   *
   * Vai direto para `trips.route_geojson` e para uma fonte do MapLibre, sem
   * conversão. É por isso que o provedor escolhido devolve GeoJSON nativamente.
   */
  geometry: { type: 'LineString'; coordinates: [number, number][] }
  roadKm: number
  minutes: number
}

/**
 * Uma função, um retorno, e `null` como ÚNICA forma de falha.
 *
 * `null` cobre tudo: sem chave, rede fora, cota estourada, resposta inesperada.
 * Isso faz o modo degradado ser um `if` no chamador, e não um segundo caminho de
 * código — o produto continua funcionando com a estimativa de sinuosidade que o
 * domínio já calcula.
 *
 * Implementação NUNCA lança.
 */
export interface RoutingClient {
  route(points: readonly Coordinates[]): Promise<RoutedLine | null>
}
```

- [ ] **Step 2: Escrever a implementação do OpenRouteService**

Crie `src/lib/routing/open-route-service.ts`:

```ts
import type { Coordinates } from '@/domain/geo'
import type { RoutedLine, RoutingClient } from './routing-client'

const ENDPOINT =
  'https://api.openrouteservice.org/v2/directions/driving-car/geojson'

/** Acima disso o provedor recusa, e a viagem de um dia nunca chega perto. */
const MAX_POINTS = 50

/** Rede lenta não pode travar o salvamento de uma viagem. */
const TIMEOUT_MS = 8000

interface OrsResponse {
  features?: {
    geometry?: { type?: string; coordinates?: unknown }
    properties?: { summary?: { distance?: unknown; duration?: unknown } }
  }[]
}

function isCoordinatePairs(value: unknown): value is [number, number][] {
  return (
    Array.isArray(value) &&
    value.every(
      (pair) =>
        Array.isArray(pair) &&
        pair.length >= 2 &&
        typeof pair[0] === 'number' &&
        typeof pair[1] === 'number',
    )
  )
}

/**
 * Traduz a resposta do ORS. Devolve `null` a qualquer sinal de que o corpo não
 * é o que se espera — em vez de confiar num `as` e explodir mais adiante.
 */
function toRoutedLine(body: unknown): RoutedLine | null {
  const feature = (body as OrsResponse)?.features?.[0]
  const coordinates = feature?.geometry?.coordinates
  const summary = feature?.properties?.summary

  if (feature?.geometry?.type !== 'LineString') return null
  if (!isCoordinatePairs(coordinates)) return null
  if (typeof summary?.distance !== 'number') return null
  if (typeof summary?.duration !== 'number') return null

  return {
    geometry: { type: 'LineString', coordinates },
    roadKm: summary.distance / 1000,
    minutes: summary.duration / 60,
  }
}

export function createOpenRouteServiceClient(apiKey: string): RoutingClient {
  return {
    async route(points) {
      if (points.length < 2 || points.length > MAX_POINTS) return null

      try {
        const response = await fetch(ENDPOINT, {
          method: 'POST',
          headers: {
            Authorization: apiKey,
            'Content-Type': 'application/json',
          },
          // O ORS recebe `[longitude, latitude]`, na ordem inversa da nossa.
          body: JSON.stringify({
            coordinates: points.map((p: Coordinates) => [
              p.longitude,
              p.latitude,
            ]),
          }),
          signal: AbortSignal.timeout(TIMEOUT_MS),
        })

        if (!response.ok) return null
        return toRoutedLine(await response.json())
      } catch {
        // Rede, timeout, JSON inválido: tudo vira `null`. O chamador cai no
        // modo estimado, e o usuário perde precisão, não a viagem.
        return null
      }
    },
  }
}
```

- [ ] **Step 3: Escrever a fábrica**

Crie `src/lib/routing/index.ts`:

```ts
import { createOpenRouteServiceClient } from './open-route-service'
import type { RoutingClient } from './routing-client'

/**
 * `null` quando não há chave configurada — e o produto continua funcionando em
 * modo estimado, do mesmo jeito que a tela de entrada lida com Supabase ausente.
 *
 * A variável NÃO tem prefixo `NEXT_PUBLIC_`, e isso é o ponto todo: a chamada ao
 * provedor acontece só no servidor, e a chave nunca chega ao navegador.
 */
export function getRoutingClient(): RoutingClient | null {
  const apiKey = process.env.OPENROUTESERVICE_API_KEY
  if (!apiKey) return null
  return createOpenRouteServiceClient(apiKey)
}

export type { RoutedLine, RoutingClient } from './routing-client'
```

- [ ] **Step 4: Escrever o ADR**

Crie `docs/decisions/0013-openrouteservice-para-tracado.md`:

```markdown
# 0013 — OpenRouteService para o traçado da estrada

**Adiciona** uma dependência externa de serviço. **Não altera** o
[ADR 0001](./0001-stack-e-limite-de-dependencias.md): nenhum pacote novo entra no
`package.json` — é uma chamada HTTP a um serviço, atrás de uma interface de uma
função.

## Contexto

O Rastro passa a montar roteiros. A escolha e a ordem das paradas são domínio
próprio (`src/domain/itinerary.ts`), mas desenhar a linha que segue a estrada de
verdade exige uma malha viária roteável, que não temos.

A alternativa é a estimativa que já existe: linha reta vezes o fator de
sinuosidade de 1,35. Ela erra em serra, que é justamente onde este produto vive.

## Decisão

Usar o **OpenRouteService** para obter geometria, distância e duração reais,
atrás da interface `RoutingClient`, que tem **uma função** e devolve `null` como
única forma de falha.

## Motivos

- **2.000 requisições/dia no plano gratuito.** Uma pessoa planeja um punhado de
  viagens; a cota nunca será tocada.
- **Devolve GeoJSON nativamente**, que cai direto em `trips.route_geojson` e numa
  fonte do MapLibre sem conversão.
- **Dados OpenStreetMap**, a mesma origem dos tiles do MapTiler que já usamos.
- **Não amarra a qual mapa se desenha por cima.**

## Alternativas descartadas

| Alternativa | Por que não |
|---|---|
| Mapbox Directions | Há indício de que os termos proíbem usar o resultado sobre mapa de outro fornecedor, e desenhamos em MapTiler. **NÃO VERIFICADO** — ver Pendências. Descartada também por não ser necessária: o ORS resolve. |
| Servidor de demonstração do OSRM | Declaradamente não é para produção. |
| OSRM auto-hospedado | Operar malha viária própria para um app pessoal é desproporcional. |
| Endpoint de otimização do provedor | Ordenaria as paradas por **distância**. A ordem que interessa aqui é por **interesse**, e isso é regra de negócio — tem de viver em `src/domain/`, não numa API de terceiro. |

## Consequências

- **A chamada é sempre do servidor.** `OPENROUTESERVICE_API_KEY` **sem** o prefixo
  `NEXT_PUBLIC_`.
- **Sem a chave o produto funciona.** `getRoutingClient()` devolve `null` e a
  viagem é salva com os números estimados.
- **`route_geojson is null` é o sinalizador de "este número é estimado".** Nenhuma
  coluna nova, e a interface é obrigada a dizer qual dos dois o usuário está
  vendo — pelo mesmo princípio que impede apresentar dado de `src/mocks/` como
  verificado.
- **Sem cache de rota.** A cota não será tocada; inventar chave de cache seria
  complexidade sem comprador.

## Pendência

- [ ] Ler os termos da Mapbox Directions API e registrar aqui o que eles de fato
      dizem. A afirmação acima veio de conversa e **não foi verificada**. Não muda
      a decisão, mas não deve ser repetida como fato.

## Gatilho de revisão

Se o traçado passar a ser pedido em volume — importação em lote, recálculo a cada
edição de parada — a cota volta à mesa e o cache deixa de ser complexidade sem
comprador.
```

- [ ] **Step 5: Documentar a variável de ambiente**

Se `.env.example` existir, acrescente ao fim; se não, crie com este conteúdo:

```
# Traçado real da estrada nos roteiros. Opcional: sem ela o Rastro usa a
# estimativa de sinuosidade e diz na tela que a distância é estimada.
# Chave gratuita em https://openrouteservice.org/dev/#/signup
# SEM prefixo NEXT_PUBLIC_: a chamada acontece só no servidor.
OPENROUTESERVICE_API_KEY=
```

- [ ] **Step 6: Confirmar que nenhuma dependência entrou e que a camada está respeitada**

Run: `git diff --stat package.json package-lock.json`
Expected: sem saída — nenhum pacote novo.

Run: `npx eslint src/lib/routing --max-warnings=0`
Expected: limpo.

Confirme a mão que `src/lib/routing/*` importa **apenas** de `@/domain/*` e de dentro de `src/lib/routing/`. Nada de `components`, `lib/data` ou `lib/supabase`.

- [ ] **Step 7: Rodar lint e typecheck**

Run: `npm run lint && npm run typecheck`
Expected: limpos.

- [ ] **Step 8: Escrever no CHANGELOG e commitar**

Sob `### Adicionado`:

```markdown
- O traçado de um roteiro passa a seguir a estrada de verdade, com quilometragem
  e tempo medidos em vez de estimados. Sem a chave do serviço, ou se ele estiver
  fora do ar, a viagem continua funcionando com a estimativa — e a tela diz qual
  das duas você está vendo
```

```bash
git add src/lib/routing docs/decisions/0013-openrouteservice-para-tracado.md .env.example CHANGELOG.md
git commit -m "feat: o tracado segue a estrada de verdade, e sobrevive sem ela"
```

---

# Fase 3 — Persistência

### Task 6: Tradução linha ↔ domínio, e a conversão de data

**Files:**
- Create: `src/lib/data/supabase/trip-row.ts`
- Create: `src/lib/data/supabase/trip-row.test.ts`
- Modify: `src/domain/trip.ts` (acrescentar os modelos de leitura)

**Interfaces:**
- Consumes: `Trip`, `TripStop`, `TripStatus` de `src/domain/trip.ts`; `Coordinates` de `geo.ts`.
- Produces: em `src/domain/trip.ts` — `TripSummary`, `TripDetail`, `TripDetailStop`. Em `src/domain/dates.ts` — `civilDateInTimeZone(instantIso: string, timeZone: string): string`. Em `trip-row.ts` — `TRIP_SELECT`, `TripRow`, `TripStopRow`, `toTripSummaryFromRow(row: TripRow): TripSummary`, `toTripDetailFromRow(row: TripRow): TripDetail`.

> **`civilDateInTimeZone` vive em `src/domain/dates.ts`, não no adapter.** É função pura de calendário, e `dates.ts` já é o arquivo que documenta exatamente esta mordida. Deixá-la no adapter obrigaria as Server Actions a importar de dentro de `lib/data/supabase/`, alcançando o interior de um adapter para pegar uma função que não tem nada de Supabase.

- [ ] **Step 1: Acrescentar os modelos de leitura ao domínio**

Em `src/domain/trip.ts`, ao fim do arquivo:

```ts
/** Uma parada já persistida, com o lugar do catálogo quando houver. */
export interface TripDetailStop {
  id: string
  placeId: string | null
  orderIndex: number
  label: string
  coordinates: Coordinates
  kind: TripStopKind
  /** Preenchido ao concluir a viagem, para as paradas confirmadas. */
  arrivedAt: string | null
}

/** O que a lista de viagens precisa. Sem paradas: a lista não as mostra. */
export interface TripSummary {
  id: string
  slug: string
  title: string
  status: TripStatus
  startedAt: string | null
  endedAt: string | null
  distanceKm: number | null
  durationMinutes: number | null
  stopCount: number
  /**
   * `false` quando `route_geojson` é nulo: os números vieram do fator de
   * sinuosidade, não de malha viária. A interface é obrigada a dizer isso.
   */
  hasRealRoute: boolean
}

export interface TripDetail extends TripSummary {
  originLabel: string | null
  originCoordinates: Coordinates | null
  notes: string | null
  stops: TripDetailStop[]
  /** `LineString` do traçado real, quando houver. */
  routeGeoJson: {
    type: 'LineString'
    coordinates: [number, number][]
  } | null
}
```

- [ ] **Step 2: A conversão de data, no domínio, com teste primeiro**

Acrescente a `src/domain/dates.test.ts`:

```ts
import { civilDateInTimeZone } from './dates'

describe('civilDateInTimeZone', () => {
  it('às 23h30 de Brasília, a data é o dia da viagem, não o dia seguinte em UTC', () => {
    // 2026-07-30T23:30 em -03:00 é 2026-07-31T02:30Z.
    expect(
      civilDateInTimeZone('2026-07-31T02:30:00.000Z', 'America/Sao_Paulo'),
    ).toBe('2026-07-30')
  })

  it('à 01h de Brasília, continua sendo o dia em Brasília', () => {
    expect(
      civilDateInTimeZone('2026-07-30T04:00:00.000Z', 'America/Sao_Paulo'),
    ).toBe('2026-07-30')
  })

  it('em UTC, a data é a própria', () => {
    expect(civilDateInTimeZone('2026-07-31T02:30:00.000Z', 'UTC')).toBe(
      '2026-07-31',
    )
  })
})
```

Run: `npx vitest run src/domain/dates.test.ts`
Expected: FAIL — `civilDateInTimeZone is not a function`.

Agora acrescente a `src/domain/dates.ts`:

```ts
/**
 * Data civil `YYYY-MM-DD` de um instante, no fuso informado.
 *
 * Existe porque `place_visits.visited_at` é `date` e `trips.ended_at` é
 * `timestamptz`. Converter no fuso errado registra a visita no dia ANTERIOR: uma
 * viagem encerrada às 23h30 em Brasília é 02h30 do dia seguinte em UTC, e a
 * memória apareceria no mapa na data errada.
 *
 * É a mesma armadilha que o `formatVisitDate` acima já documenta, agora no
 * sentido inverso. `Intl` sabe fuso e horário de verão; fazer a conta com
 * deslocamento fixo de -3 horas voltaria a errar duas vezes por ano.
 */
export function civilDateInTimeZone(
  instantIso: string,
  timeZone: string,
): string {
  // `en-CA` formata como `YYYY-MM-DD`, que é exatamente o formato do banco.
  return new Intl.DateTimeFormat('en-CA', {
    timeZone,
    year: 'numeric',
    month: '2-digit',
    day: '2-digit',
  }).format(new Date(instantIso))
}
```

Run: `npx vitest run src/domain/dates.test.ts`
Expected: PASS.

- [ ] **Step 3: Escrever o teste da tradução de linha**

Crie `src/lib/data/supabase/trip-row.test.ts`:

```ts
import { describe, expect, it } from 'vitest'
import { toTripSummaryFromRow } from './trip-row'
import type { TripRow } from './trip-row'

function row(overrides: Partial<TripRow> = {}): TripRow {
  return {
    id: 't1',
    slug: 'serra-do-rio-do-rastro',
    title: 'Serra do Rio do Rastro',
    status: 'planned',
    started_at: null,
    ended_at: null,
    origin_label: 'Palhoça, SC',
    origin_latitude: -27.6455,
    origin_longitude: -48.67,
    distance_km: 184.5,
    duration_minutes: 220,
    notes: null,
    route_geojson: null,
    trip_stops: [],
    ...overrides,
  }
}

describe('toTripSummaryFromRow', () => {
  it('sem traçado gravado, marca os números como estimados', () => {
    expect(toTripSummaryFromRow(row()).hasRealRoute).toBe(false)
  })

  it('com traçado gravado, marca os números como medidos', () => {
    const summary = toTripSummaryFromRow(
      row({
        route_geojson: {
          type: 'LineString',
          coordinates: [
            [-48.67, -27.64],
            [-49.4, -28.1],
          ],
        },
      }),
    )

    expect(summary.hasRealRoute).toBe(true)
  })

  it('conta as paradas', () => {
    const summary = toTripSummaryFromRow(
      row({
        trip_stops: [
          { id: 's1', place_id: null, order_index: 0, label: 'A', latitude: -28, longitude: -49, kind: 'waypoint', arrived_at: null },
          { id: 's2', place_id: null, order_index: 1, label: 'B', latitude: -28.2, longitude: -49.2, kind: 'waypoint', arrived_at: null },
        ],
      }),
    )

    expect(summary.stopCount).toBe(2)
  })

  it('numérico do Postgres chega como string e vira número', () => {
    // `numeric(8,2)` volta do PostgREST como string. Sem coerção, a interface
    // formataria "184.50" em vez de 184,5 — e somas dariam concatenação.
    const summary = toTripSummaryFromRow(
      row({ distance_km: '184.50' as unknown as number }),
    )

    expect(summary.distanceKm).toBe(184.5)
  })
})
```

- [ ] **Step 4: Rodar o teste para confirmar que falha**

Run: `npx vitest run src/lib/data/supabase/trip-row.test.ts`
Expected: FAIL — `Failed to resolve import "./trip-row"`.

- [ ] **Step 5: Escrever a implementação**

Crie `src/lib/data/supabase/trip-row.ts`:

```ts
import type {
  TripDetail,
  TripDetailStop,
  TripStatus,
  TripStopKind,
  TripSummary,
} from '@/domain/trip'

/**
 * Uma projeção só. A lista e o detalhe leem as mesmas colunas: o embed de
 * paradas é barato, e duas projeções divergiriam em silêncio.
 */
export const TRIP_SELECT = `
  id, slug, title, status, started_at, ended_at,
  origin_label, origin_latitude, origin_longitude,
  distance_km, duration_minutes, notes, route_geojson,
  trip_stops (id, place_id, order_index, label, latitude, longitude, kind, arrived_at)
`

export interface TripStopRow {
  id: string
  place_id: string | null
  order_index: number
  label: string
  latitude: number | null
  longitude: number | null
  kind: TripStopKind
  arrived_at: string | null
}

export interface TripRow {
  id: string
  slug: string
  title: string
  status: TripStatus
  started_at: string | null
  ended_at: string | null
  origin_label: string | null
  origin_latitude: number | null
  origin_longitude: number | null
  distance_km: number | null
  duration_minutes: number | null
  notes: string | null
  route_geojson: {
    type: 'LineString'
    coordinates: [number, number][]
  } | null
  trip_stops: TripStopRow[]
}

/**
 * `numeric` do Postgres chega do PostgREST como string, para não perder
 * precisão. Sem esta coerção a interface formataria "184.50" e qualquer soma
 * viraria concatenação.
 */
function toNumber(value: number | null): number | null {
  if (value === null) return null
  const parsed = typeof value === 'number' ? value : Number(value)
  return Number.isFinite(parsed) ? parsed : null
}

export function toTripSummaryFromRow(row: TripRow): TripSummary {
  return {
    id: row.id,
    slug: row.slug,
    title: row.title,
    status: row.status,
    startedAt: row.started_at,
    endedAt: row.ended_at,
    distanceKm: toNumber(row.distance_km),
    durationMinutes: toNumber(row.duration_minutes),
    stopCount: row.trip_stops.length,
    hasRealRoute: row.route_geojson !== null,
  }
}

function toDetailStop(row: TripStopRow): TripDetailStop {
  return {
    id: row.id,
    placeId: row.place_id,
    orderIndex: row.order_index,
    label: row.label,
    coordinates: {
      latitude: row.latitude ?? 0,
      longitude: row.longitude ?? 0,
    },
    kind: row.kind,
    arrivedAt: row.arrived_at,
  }
}

export function toTripDetailFromRow(row: TripRow): TripDetail {
  return {
    ...toTripSummaryFromRow(row),
    originLabel: row.origin_label,
    originCoordinates:
      row.origin_latitude !== null && row.origin_longitude !== null
        ? { latitude: row.origin_latitude, longitude: row.origin_longitude }
        : null,
    notes: row.notes,
    stops: [...row.trip_stops]
      .sort((a, b) => a.order_index - b.order_index)
      .map(toDetailStop),
    routeGeoJson: row.route_geojson,
  }
}
```

- [ ] **Step 6: Rodar os dois arquivos de teste**

Run: `npx vitest run src/lib/data/supabase/trip-row.test.ts src/domain/dates.test.ts`
Expected: PASS — 4 de `trip-row` e 3 novos de `dates`, mais os que `dates.test.ts` já tinha.

- [ ] **Step 7: Rodar lint e typecheck**

Run: `npm run lint && npm run typecheck`
Expected: limpos.

- [ ] **Step 8: Escrever no CHANGELOG e commitar**

Sob `### Corrigido`:

```markdown
- A data de uma visita gerada por viagem é o dia em que você viajou, no seu fuso —
  não o dia seguinte. Uma viagem encerrada às 23h30 em Brasília já é o dia
  seguinte em UTC, e converter errado registraria a memória na data errada
```

```bash
git add src/domain/trip.ts src/domain/dates.ts src/domain/dates.test.ts src/lib/data/supabase/trip-row.ts src/lib/data/supabase/trip-row.test.ts CHANGELOG.md
git commit -m "feat: traducao de viagem, com a data no fuso de quem viajou"
```

---

### Task 7: Repositório de viagens

**Files:**
- Create: `src/lib/data/trip-repository.ts`
- Create: `src/lib/data/supabase/supabase-trip-repository.ts`
- Modify: `src/lib/data/index.ts`

**Interfaces:**
- Consumes: `TripSummary`, `TripDetail` de `src/domain/trip.ts`; `TRIP_SELECT`, `toTripSummaryFromRow`, `toTripDetailFromRow`, `TripRow` de `./supabase/trip-row`; `Coordinates` de `geo.ts`.
- Produces: `NewTripInput`, `NewTripStopInput`, `TripRepository`, `createSupabaseTripRepository(supabase, userId): TripRepository`, `getTripRepository(): Promise<TripRepository>`.

- [ ] **Step 1: Escrever o contrato**

Crie `src/lib/data/trip-repository.ts`:

```ts
import type { Coordinates } from '@/domain/geo'
import type { TripDetail, TripStopKind, TripSummary } from '@/domain/trip'

export interface NewTripStopInput {
  placeId: string | null
  label: string
  coordinates: Coordinates
  kind: TripStopKind
}

export interface NewTripInput {
  title: string
  originLabel: string | null
  originCoordinates: Coordinates
  /** Já na ordem de rodagem: o índice é a posição no array. */
  stops: NewTripStopInput[]
  /** Medidos quando houver traçado; estimados quando não. */
  distanceKm: number
  durationMinutes: number
  /** `null` quando o roteamento falhou — e é o sinal de "número estimado". */
  routeGeoJson: {
    type: 'LineString'
    coordinates: [number, number][]
  } | null
}

/**
 * Contrato de viagens.
 *
 * Nenhum método recebe `userId` para filtrar: a política `trips_own` filtra por
 * `auth.uid()`, e `trip_stops_via_trip` herda a posse da viagem. Ver ADR 0008.
 */
export interface TripRepository {
  listTrips(): Promise<TripSummary[]>
  getBySlug(slug: string): Promise<TripDetail | null>
  listSlugs(): Promise<string[]>
  createTrip(input: NewTripInput, slug: string): Promise<TripDetail>
  deleteTrip(id: string): Promise<void>
  /**
   * Conclui a viagem e registra visita nas paradas confirmadas, numa transação.
   *
   * `visitedAt` é data civil `YYYY-MM-DD`, no fuso de quem viajou.
   */
  completeTrip(
    tripId: string,
    confirmedStopIds: readonly string[],
    visitedAt: string,
  ): Promise<void>
}
```

- [ ] **Step 2: Escrever o adapter**

Crie `src/lib/data/supabase/supabase-trip-repository.ts`:

```ts
import type { SupabaseClient } from '@supabase/supabase-js'
import type { NewTripInput, TripRepository } from '../trip-repository'
import {
  TRIP_SELECT,
  toTripDetailFromRow,
  toTripSummaryFromRow,
  type TripRow,
} from './trip-row'

function toTripColumns(input: NewTripInput, slug: string, userId: string) {
  return {
    user_id: userId,
    title: input.title.trim(),
    slug,
    status: 'planned' as const,
    origin_label: input.originLabel?.trim() || null,
    origin_latitude: input.originCoordinates.latitude,
    origin_longitude: input.originCoordinates.longitude,
    distance_km: input.distanceKm,
    duration_minutes: Math.round(input.durationMinutes),
    route_geojson: input.routeGeoJson,
  }
}

export function createSupabaseTripRepository(
  supabase: SupabaseClient,
  userId: string,
): TripRepository {
  return {
    async listTrips() {
      const { data, error } = await supabase
        .from('trips')
        .select(TRIP_SELECT)
        .order('started_at', { ascending: false, nullsFirst: false })
        .order('created_at', { ascending: false })

      if (error) throw new Error(error.message)
      return (data as unknown as TripRow[]).map(toTripSummaryFromRow)
    },

    async getBySlug(slug) {
      const { data, error } = await supabase
        .from('trips')
        .select(TRIP_SELECT)
        .eq('slug', slug)
        .maybeSingle()

      if (error) throw new Error(error.message)
      if (!data) return null
      return toTripDetailFromRow(data as unknown as TripRow)
    },

    async listSlugs() {
      const { data, error } = await supabase.from('trips').select('slug')
      if (error) throw new Error(error.message)
      return (data as { slug: string }[]).map((row) => row.slug)
    },

    async createTrip(input, slug) {
      const { data: trip, error } = await supabase
        .from('trips')
        .insert(toTripColumns(input, slug, userId))
        .select('id')
        .single()

      if (error) throw new Error(error.message)
      const tripId = (trip as { id: string }).id

      if (input.stops.length > 0) {
        const { error: stopsError } = await supabase.from('trip_stops').insert(
          input.stops.map((stop, index) => ({
            trip_id: tripId,
            place_id: stop.placeId,
            order_index: index,
            label: stop.label,
            latitude: stop.coordinates.latitude,
            longitude: stop.coordinates.longitude,
            kind: stop.kind,
          })),
        )

        // A viagem sem paradas é inútil. Apagar o cabeçalho é preferível a
        // deixar uma viagem vazia no histórico de quem não pediu isso.
        if (stopsError) {
          await supabase.from('trips').delete().eq('id', tripId)
          throw new Error(stopsError.message)
        }
      }

      const { data, error: readError } = await supabase
        .from('trips')
        .select(TRIP_SELECT)
        .eq('id', tripId)
        .single()

      if (readError) throw new Error(readError.message)
      return toTripDetailFromRow(data as unknown as TripRow)
    },

    async deleteTrip(id) {
      const { error } = await supabase.from('trips').delete().eq('id', id)
      if (error) throw new Error(error.message)
    },

    /**
     * Uma chamada, uma transação. Escrever `trips` e `place_visits` em dois
     * `await` deixaria o banco mentindo de forma VISÍVEL na falha do meio: é o
     * registro de visita que pinta o pin de verde no mapa.
     */
    async completeTrip(tripId, confirmedStopIds, visitedAt) {
      const { error } = await supabase.rpc('complete_trip', {
        p_trip_id: tripId,
        p_confirmed_stop_ids: confirmedStopIds,
        p_visited_at: visitedAt,
      })

      if (error) throw new Error(error.message)
    },
  }
}
```

- [ ] **Step 3: Ligar no `index.ts`**

Em `src/lib/data/index.ts`, acrescente o import junto dos outros (ordem alfabética entre os `./supabase/…`) e a função junto das demais:

```ts
import type { TripRepository } from './trip-repository'
import { createSupabaseTripRepository } from './supabase/supabase-trip-repository'
```

```ts
export async function getTripRepository(): Promise<TripRepository> {
  const { supabase, userId } = await sessionContext()
  return createSupabaseTripRepository(supabase, userId)
}
```

E acrescente `TripRepository` à lista do `export type { … }` no fim do arquivo.

- [ ] **Step 4: Rodar lint e typecheck**

Run: `npm run lint && npm run typecheck`
Expected: limpos.

- [ ] **Step 5: Confirmar que nenhum repositório filtra por `user_id`**

Run: `npx --no-install grep -rn "eq('user_id'" src/lib/data || true`

Se preferir a ferramenta do editor: busque `eq('user_id'` em `src/lib/data`.
Expected: **nenhum resultado.** A RLS filtra; repetir o filtro na consulta é o que o ADR 0008 proíbe.

- [ ] **Step 6: Escrever no CHANGELOG e commitar**

Sob `### Adicionado`:

```markdown
- Viagens passam a ser salvas de verdade, com as paradas na ordem e o traçado
```

```bash
git add src/lib/data/trip-repository.ts src/lib/data/supabase/supabase-trip-repository.ts src/lib/data/index.ts CHANGELOG.md
git commit -m "feat: repositorio de viagens, sem repetir o filtro que a RLS ja faz"
```

---

### Task 8: Migration 0003 — a escrita atômica, e a prova de RLS

**Files:**
- Create: `supabase/migrations/0003_complete_trip.sql`
- Create: `scripts/verificar-complete-trip-rls.mjs`
- Modify: `docs/VERIFICACAO-RLS.md`

**Interfaces:**
- Consumes: tabelas `trips`, `trip_stops`, `place_visits` da migration 0001.
- Produces: função Postgres `complete_trip(p_trip_id uuid, p_confirmed_stop_ids uuid[], p_visited_at date)`, chamada por `TripRepository.completeTrip`.

- [ ] **Step 1: Escrever a migration**

Crie `supabase/migrations/0003_complete_trip.sql`:

```sql
-- Conclui uma viagem e registra visita nas paradas confirmadas, atomicamente.
--
-- Existe como função, e não como duas chamadas do aplicativo, porque a escrita
-- toca DUAS tabelas: `trips` (status e ended_at) e `place_visits` (uma linha por
-- parada confirmada). Em dois `await` separados, uma falha no meio deixaria o
-- banco mentindo de forma VISÍVEL — é o registro de visita que pinta o pin de
-- verde no mapa, então a inconsistência apareceria na tela.
--
-- `security invoker` é deliberado: a RLS continua valendo DENTRO da função, e o
-- banco segue sendo a única autoridade de autorização (ADR 0008). Com
-- `security definer` esta função passaria por cima das políticas e viraria um
-- buraco pelo qual um usuário concluiria a viagem de outro.
create or replace function complete_trip(
  p_trip_id uuid,
  p_confirmed_stop_ids uuid[],
  p_visited_at date
)
returns void
language plpgsql
security invoker
set search_path = public
as $$
declare
  v_updated integer;
begin
  update trips
     set status = 'completed',
         ended_at = coalesce(ended_at, now()),
         updated_at = now()
   where id = p_trip_id;

  get diagnostics v_updated = row_count;

  -- Zero linhas significa que a RLS recusou, ou que a viagem não existe. Falhar
  -- aqui evita gravar visitas órfãs numa viagem que não é de quem pediu.
  if v_updated = 0 then
    raise exception 'viagem nao encontrada ou sem permissao';
  end if;

  -- Marca as paradas que aconteceram. As não confirmadas ficam com arrived_at
  -- nulo: o roteiro planejado permanece registrado, e a memória diz só o que de
  -- fato houve.
  update trip_stops
     set arrived_at = p_visited_at
   where trip_id = p_trip_id
     and id = any(p_confirmed_stop_ids);

  -- Só parada confirmada E ligada ao catálogo vira visita. Parada solta
  -- (combustível, almoço) não é lugar e não entra na memória do mapa.
  --
  -- O `not exists` torna a chamada idempotente: concluir duas vezes não duplica
  -- a visita, e o trigger de visit_count não conta duas passagens onde houve uma.
  insert into place_visits (user_id, place_id, trip_id, visited_at)
  select s.user_id_of_trip, s.place_id, p_trip_id, p_visited_at
    from (
      select t.user_id as user_id_of_trip, ts.place_id
        from trip_stops ts
        join trips t on t.id = ts.trip_id
       where ts.trip_id = p_trip_id
         and ts.id = any(p_confirmed_stop_ids)
         and ts.place_id is not null
    ) s
   where not exists (
     select 1 from place_visits pv
      where pv.trip_id = p_trip_id
        and pv.place_id = s.place_id
   );
end;
$$;

comment on function complete_trip is
  'Conclui viagem e registra visitas confirmadas numa transacao. security invoker: a RLS decide.';
```

- [ ] **Step 2: Aplicar a migration**

Cole o conteúdo do arquivo no SQL Editor do Supabase e execute.
Expected: `Success. No rows returned`.

- [ ] **Step 3: Escrever a prova de RLS**

Crie `scripts/verificar-complete-trip-rls.mjs`. Ele usa **apenas** a chave `anon` — a `service_role` nunca entra neste repositório.

```js
/**
 * Prova que `complete_trip` NÃO pode ser usada por quem não é dono da viagem.
 *
 * A função é `security invoker` justamente para isso. Este script existe porque
 * "confio que a RLS pega" não é verificação — e `docs/VERIFICACAO-RLS.md` nunca
 * havia sido executado neste projeto.
 *
 * Uso: node scripts/verificar-complete-trip-rls.mjs <uuid-de-uma-viagem-real>
 * A URL e a chave anon saem de .env.local.
 */
import { readFileSync } from 'node:fs'
import { createClient } from '@supabase/supabase-js'

const env = Object.fromEntries(
  readFileSync('.env.local', 'utf8')
    .split('\n')
    .filter((line) => line.includes('=') && !line.trim().startsWith('#'))
    .map((line) => {
      const at = line.indexOf('=')
      return [line.slice(0, at).trim(), line.slice(at + 1).trim()]
    }),
)

const url = env.NEXT_PUBLIC_SUPABASE_URL
const anonKey = env.NEXT_PUBLIC_SUPABASE_ANON_KEY
const tripId = process.argv[2]

if (!url || !anonKey) throw new Error('faltam NEXT_PUBLIC_SUPABASE_* em .env.local')
if (!tripId) throw new Error('informe o uuid de uma viagem: node scripts/... <uuid>')

// Cliente anônimo: nenhuma sessão, logo auth.uid() é nulo.
const anon = createClient(url, anonKey)

const { error } = await anon.rpc('complete_trip', {
  p_trip_id: tripId,
  p_confirmed_stop_ids: [],
  p_visited_at: '2026-07-30',
})

if (!error) {
  console.error('FALHOU: anonimo concluiu a viagem. A funcao esta furada.')
  process.exit(1)
}

console.log('OK: anonimo recusado ->', error.message)

const { data, error: readError } = await anon
  .from('trips')
  .select('id, status')
  .eq('id', tripId)

if (readError) {
  console.log('OK: anonimo nao le trips ->', readError.message)
} else if (data.length === 0) {
  console.log('OK: anonimo nao ve nenhuma viagem (RLS filtrou)')
} else {
  console.error('FALHOU: anonimo leu trips', data)
  process.exit(1)
}
```

- [ ] **Step 4: Rodar a prova**

Pegue o `id` de uma viagem existente (SQL Editor: `select id from trips limit 1`). Se ainda não houver viagem, use qualquer UUID válido — o teste continua válido, porque o anônimo deve ser recusado de qualquer forma.

Run: `node scripts/verificar-complete-trip-rls.mjs <uuid>`
Expected: duas linhas começando com `OK:`. Saída zero.

Se aparecer `FALHOU`, **pare**: a função está furada e nenhuma outra tarefa deve seguir. Verifique se `security invoker` está de fato na definição.

- [ ] **Step 5: Registrar a verificação**

Em `docs/VERIFICACAO-RLS.md`, acrescente ao fim:

```markdown
## `complete_trip` — verificada

A função é `security invoker` de propósito: a RLS decide dentro dela. Com
`security definer` ela passaria por cima das políticas e um usuário concluiria a
viagem de outro.

Prova automatizada: `scripts/verificar-complete-trip-rls.mjs`, que usa **só a
chave anon** e confirma duas coisas — o anônimo é recusado ao chamar a função, e
não enxerga nenhuma linha de `trips`.

```
node scripts/verificar-complete-trip-rls.mjs <uuid-de-uma-viagem>
```

Rode isto sempre que alterar a função ou qualquer política de `trips`,
`trip_stops` ou `place_visits`.
```

- [ ] **Step 6: Rodar lint e typecheck**

Run: `npm run lint && npm run typecheck`
Expected: limpos. Se o ESLint reclamar do `.mjs` em `scripts/`, confirme se o projeto já ignora essa pasta; se não, e se outros `verify-*.mjs` vivem fora do repositório, mantenha o script em `scripts/` e ajuste o ignore do ESLint — não desligue a regra globalmente.

- [ ] **Step 7: Escrever no CHANGELOG e commitar**

Sob `### Adicionado`:

```markdown
- Concluir uma viagem e registrar as visitas dela acontece de uma vez só: ou as
  duas coisas gravam, ou nenhuma. Como é a visita que pinta o pin de verde no
  mapa, gravar pela metade apareceria na tela
```

Sob `### Segurança`:

```markdown
- Verificado que ninguém pode concluir a viagem de outra pessoa, com prova
  automatizada rodando apenas com chave pública
```

```bash
git add supabase/migrations/0003_complete_trip.sql scripts/verificar-complete-trip-rls.mjs docs/VERIFICACAO-RLS.md CHANGELOG.md
git commit -m "feat: concluir viagem e atomico, e a RLS decide dentro da funcao"
```

---

# Fase 4 — Ações

### Task 9: Server Actions de viagem

**Files:**
- Create: `src/app/actions/trip-actions.ts`

**Interfaces:**
- Consumes: `buildItinerary`, `isRefusal`, `orderAndMeasure`, `ItineraryRequest` de `@/domain/itinerary`; `slugify`, `uniqueSlug` de `@/domain/slug`; `getTripRepository`, `getPlaceRepository`, `getProfileRepository` de `@/lib/data`; `getRoutingClient` de `@/lib/routing`; `civilDateInTimeZone` de `@/lib/data/supabase/trip-row`; `ActionResult` de `./result`.
- Produces: `ITINERARY_REFUSAL_MESSAGES`, `proposeTripAction`, `createTripAction`, `completeTripAction`, `deleteTripAction`.

- [ ] **Step 1: Escrever as mensagens de recusa no domínio**

Em `src/domain/itinerary.ts`, ao fim:

```ts
/**
 * Uma mensagem por recusa, e nunca a mesma para duas.
 *
 * Dizer "aumente o raio" quando o problema é tempo mandaria o usuário para o
 * lado errado — e devolver "nenhum resultado" seco devolveria a ele um trabalho
 * que a máquina já fez.
 */
export const ITINERARY_REFUSAL_MESSAGES: Record<ItineraryRefusal, string> = {
  'no-candidates':
    'Nenhum lugar se encaixa nesses filtros. Amplie o raio ou tire uma categoria.',
  'budget-too-small':
    'Nada cabe nesse tempo. O problema é o relógio, não a distância — escolha mais horas.',
  'anchor-does-not-fit':
    'Esse destino sozinho já passa do tempo que você tem. Escolha mais horas ou outro destino.',
}
```

- [ ] **Step 2: Escrever as actions**

Crie `src/app/actions/trip-actions.ts`:

```ts
'use server'

import { revalidatePath } from 'next/cache'
import {
  ITINERARY_REFUSAL_MESSAGES,
  buildItinerary,
  isRefusal,
  orderAndMeasure,
  type ItineraryRequest,
} from '@/domain/itinerary'
import { civilDateInTimeZone } from '@/domain/dates'
import { slugify, uniqueSlug } from '@/domain/slug'
import { getPlaceRepository, getProfileRepository, getTripRepository } from '@/lib/data'
import { getRoutingClient } from '@/lib/routing'
import type { ActionResult } from './result'

/** Fuso do produto. Santa Catarina, e o `dates.ts` já assume o mesmo contexto. */
const TIME_ZONE = 'America/Sao_Paulo'

/** Data civil de hoje neste fuso, para a pontuação de recência. */
function today(): string {
  return civilDateInTimeZone(new Date().toISOString(), TIME_ZONE)
}

type ProposalInput = Omit<ItineraryRequest, 'origin'>

/**
 * Monta um roteiro e o devolve para revisão, sem gravar nada.
 *
 * Propor não é salvar: quem pediu uma sugestão pode simplesmente não gostar
 * dela, e criar viagem no banco a cada tentativa encheria o histórico de lixo.
 */
export async function proposeTripAction(input: ProposalInput): Promise<
  | { ok: true; stops: { id: string; name: string }[]; roadKm: number; minutes: number }
  | { ok: false; message: string }
> {
  const profileRepository = await getProfileRepository()
  const profile = await profileRepository.getProfile()

  if (!profile?.originCoordinates) {
    return {
      ok: false,
      message: 'Defina sua origem no perfil antes de montar um roteiro.',
    }
  }

  const placeRepository = await getPlaceRepository()
  const places = await placeRepository.listExplorePlaces()

  const outcome = buildItinerary(
    places,
    { ...input, origin: profile.originCoordinates },
    today(),
  )

  if (isRefusal(outcome)) {
    return { ok: false, message: ITINERARY_REFUSAL_MESSAGES[outcome.refusal] }
  }

  return {
    ok: true,
    stops: outcome.stops.map((place) => ({ id: place.id, name: place.name })),
    roadKm: outcome.totalRoadKm,
    minutes: outcome.totalRidingMinutes,
  }
}

/**
 * Grava a viagem a partir dos ids das paradas que o usuário aprovou.
 *
 * A ordem chega do domínio, não do cliente: `orderAndMeasure` reordena o que
 * vier. Confiar na ordem enviada pelo navegador deixaria a regra de ordenação
 * fora do domínio.
 */
export async function createTripAction(input: {
  title: string
  placeIds: string[]
}): Promise<ActionResult & { slug?: string }> {
  const title = input.title.trim()
  if (!title) return { ok: false, message: 'Dê um nome à viagem.' }
  if (input.placeIds.length === 0) {
    return { ok: false, message: 'Escolha pelo menos uma parada.' }
  }

  try {
    const profileRepository = await getProfileRepository()
    const profile = await profileRepository.getProfile()
    if (!profile?.originCoordinates) {
      return { ok: false, message: 'Defina sua origem no perfil.' }
    }

    const placeRepository = await getPlaceRepository()
    const all = await placeRepository.listExplorePlaces()
    const chosen = all.filter((place) => input.placeIds.includes(place.id))

    if (chosen.length === 0) {
      return { ok: false, message: 'Nenhuma das paradas escolhidas existe.' }
    }

    const itinerary = orderAndMeasure(profile.originCoordinates, chosen)

    // Traçado real quando possível. `null` cobre sem-chave, rede fora e cota
    // estourada, e nesse caso os números seguem sendo os estimados do domínio —
    // com `route_geojson` nulo, que é o sinal de "estimado" na interface.
    const routing = getRoutingClient()
    const routed = routing
      ? await routing.route([
          profile.originCoordinates,
          ...itinerary.stops,
          profile.originCoordinates,
        ])
      : null

    const repository = await getTripRepository()
    const taken = await repository.listSlugs()
    const slug = uniqueSlug(slugify(title), taken)

    const trip = await repository.createTrip(
      {
        title,
        originLabel: profile.originLabel ?? null,
        originCoordinates: profile.originCoordinates,
        stops: itinerary.stops.map((place) => ({
          placeId: place.id,
          label: place.name,
          coordinates: { latitude: place.latitude, longitude: place.longitude },
          kind: 'waypoint' as const,
        })),
        distanceKm: routed?.roadKm ?? itinerary.totalRoadKm,
        durationMinutes: routed?.minutes ?? itinerary.totalRidingMinutes,
        routeGeoJson: routed?.geometry ?? null,
      },
      slug,
    )

    revalidatePath('/', 'layout')
    return { ok: true, slug: trip.slug }
  } catch {
    return { ok: false, message: 'Não foi possível salvar a viagem.' }
  }
}

/**
 * Conclui a viagem registrando visita só nas paradas confirmadas.
 *
 * Lista vazia é permitida: você rodou e tudo estava fechado. Isso é um fato, e o
 * produto não vai discutir com ele.
 */
export async function completeTripAction(
  tripId: string,
  confirmedStopIds: string[],
): Promise<ActionResult> {
  try {
    const repository = await getTripRepository()
    await repository.completeTrip(tripId, confirmedStopIds, today())
  } catch {
    return { ok: false, message: 'Não foi possível concluir a viagem.' }
  }

  revalidatePath('/', 'layout')
  return { ok: true }
}

export async function deleteTripAction(tripId: string): Promise<ActionResult> {
  try {
    const repository = await getTripRepository()
    await repository.deleteTrip(tripId)
  } catch {
    return { ok: false, message: 'Não foi possível apagar a viagem.' }
  }

  revalidatePath('/', 'layout')
  return { ok: true }
}
```

- [ ] **Step 3: Confirmar o contrato do repositório de perfil**

O código acima assume `getProfile()` devolvendo algo com `originCoordinates` e `originLabel`.

Run: leia `src/lib/data/profile-repository.ts`.

Se os nomes divergirem, **ajuste a action ao contrato existente** — não mude o repositório para caber na action, e não invente método novo.

- [ ] **Step 4: Rodar lint, typecheck e a suíte**

Run: `npm run lint && npm run typecheck && npm test -- --run`
Expected: limpos, e os testes anteriores continuam passando.

- [ ] **Step 5: Escrever no CHANGELOG e commitar**

Sob `### Adicionado`:

```markdown
- Pedir uma sugestão de roteiro não cria viagem nenhuma: só ao salvar ela passa a
  existir. Tentar cinco combinações não enche seu histórico de rascunho
```

```bash
git add src/domain/itinerary.ts src/app/actions/trip-actions.ts CHANGELOG.md
git commit -m "feat: acoes de viagem, e propor nao e salvar"
```

---

# Fase 5 — Mapa

### Task 10: O traçado desenhado como a marca

**Files:**
- Create: `src/lib/map/trip-route-layers.ts`
- Create: `src/components/map/TripRouteLayer.tsx`

**Interfaces:**
- Consumes: `TripDetail` de `@/domain/trip`; `useMapInstance` de `@/components/map/map-context`.
- Produces: `TRIP_ROUTE_SOURCE_ID`, `TRIP_ROUTE_LAYERS`, `buildTripRouteGeoJson(detail: TripDetail)`, `buildTripRouteLayers()`, componente `TripRouteLayer`.

- [ ] **Step 1: Escrever as camadas**

Crie `src/lib/map/trip-route-layers.ts`:

```ts
import type { LineLayerSpecification } from 'maplibre-gl'
import type { TripDetail } from '@/domain/trip'

export const TRIP_ROUTE_SOURCE_ID = 'trip-route'

const ASPHALT_LAYER_ID = 'trip-route-asphalt'
const CENTERLINE_LAYER_ID = 'trip-route-centerline'

export const TRIP_ROUTE_LAYERS = [
  ASPHALT_LAYER_ID,
  CENTERLINE_LAYER_ID,
] as const

/**
 * Cores em hex literal espelhando `globals.css` à mão: MapLibre desenha em WebGL
 * e não lê variável CSS. Mudar uma sem a outra faz interface e mapa divergirem.
 * Paleta do ADR 0012.
 */
const ASPHALT = '#e0a02e'
const CENTERLINE = '#11100f'

/**
 * A rota é desenhada como a MARCA: traço âmbar com faixa central tracejada na
 * cor do fundo. É o único lugar do produto onde a geometria da identidade vira
 * informação literal.
 *
 * Também resolve a leitura: as vias do mapa base são osso (`#c2b9a7`) e os pins
 * são círculos, então a rota não compete com nenhum dos dois.
 */
export function buildTripRouteLayers(): LineLayerSpecification[] {
  return [
    {
      id: ASPHALT_LAYER_ID,
      type: 'line',
      source: TRIP_ROUTE_SOURCE_ID,
      layout: { 'line-cap': 'round', 'line-join': 'round' },
      paint: {
        'line-color': ASPHALT,
        'line-width': ['interpolate', ['linear'], ['zoom'], 6, 3, 14, 8],
      },
    },
    {
      id: CENTERLINE_LAYER_ID,
      type: 'line',
      source: TRIP_ROUTE_SOURCE_ID,
      layout: { 'line-cap': 'butt', 'line-join': 'round' },
      paint: {
        'line-color': CENTERLINE,
        'line-width': ['interpolate', ['linear'], ['zoom'], 6, 0.7, 14, 1.6],
        // `line-dasharray` é em múltiplos da largura da linha, não em pixels.
        'line-dasharray': [2, 3],
      },
    },
  ]
}

/**
 * Traçado real quando houver; senão, linha reta entre as paradas.
 *
 * A linha reta é honesta sobre o que é: sem `route_geojson` a distância também é
 * estimada, e o painel diz isso. Desenhar uma curva inventada seria pior que
 * desenhar o segmento que de fato foi medido.
 */
export function buildTripRouteGeoJson(
  detail: TripDetail,
): GeoJSON.FeatureCollection<GeoJSON.LineString> {
  const coordinates: [number, number][] = detail.routeGeoJson
    ? detail.routeGeoJson.coordinates
    : [
        ...(detail.originCoordinates
          ? ([
              [
                detail.originCoordinates.longitude,
                detail.originCoordinates.latitude,
              ],
            ] as [number, number][])
          : []),
        ...detail.stops.map(
          (stop) =>
            [stop.coordinates.longitude, stop.coordinates.latitude] as [
              number,
              number,
            ],
        ),
        ...(detail.originCoordinates
          ? ([
              [
                detail.originCoordinates.longitude,
                detail.originCoordinates.latitude,
              ],
            ] as [number, number][])
          : []),
      ]

  return {
    type: 'FeatureCollection',
    features:
      coordinates.length >= 2
        ? [
            {
              type: 'Feature',
              properties: {},
              geometry: { type: 'LineString', coordinates },
            },
          ]
        : [],
  }
}
```

- [ ] **Step 2: Escrever o componente**

Crie `src/components/map/TripRouteLayer.tsx`:

```tsx
'use client'

import { useEffect } from 'react'
import type { GeoJSONSource } from 'maplibre-gl'
import type { TripDetail } from '@/domain/trip'
import {
  TRIP_ROUTE_LAYERS,
  TRIP_ROUTE_SOURCE_ID,
  buildTripRouteGeoJson,
  buildTripRouteLayers,
} from '@/lib/map/trip-route-layers'
import { useMapInstance } from './map-context'

interface TripRouteLayerProps {
  trip: TripDetail | null
}

export function TripRouteLayer({ trip }: TripRouteLayerProps) {
  const map = useMapInstance()

  useEffect(() => {
    if (!map) return

    const data = trip
      ? buildTripRouteGeoJson(trip)
      : ({ type: 'FeatureCollection', features: [] } as const)

    function apply() {
      if (!map) return

      const existing = map.getSource(TRIP_ROUTE_SOURCE_ID) as
        | GeoJSONSource
        | undefined

      if (existing) {
        existing.setData(data as GeoJSON.FeatureCollection)
        return
      }

      map.addSource(TRIP_ROUTE_SOURCE_ID, {
        type: 'geojson',
        data: data as GeoJSON.FeatureCollection,
      })

      // Abaixo das camadas de pin, que já estão no mapa: a rota é o caminho, os
      // pins são os destinos, e destino não fica atrás de caminho.
      for (const layer of buildTripRouteLayers()) {
        map.addLayer(layer)
      }
    }

    if (map.isStyleLoaded()) apply()
    else map.once('load', apply)

    return () => {
      if (!map || !map.getSource(TRIP_ROUTE_SOURCE_ID)) return
      for (const id of TRIP_ROUTE_LAYERS) {
        if (map.getLayer(id)) map.removeLayer(id)
      }
      map.removeSource(TRIP_ROUTE_SOURCE_ID)
    }
  }, [map, trip])

  return null
}
```

- [ ] **Step 3: Rodar lint e typecheck**

Run: `npm run lint && npm run typecheck`
Expected: limpos.

- [ ] **Step 4: VERIFICAÇÃO VISUAL — obrigatória**

`lint`, `typecheck` e os testes **não desenham o mapa**. Este repositório já teve 14 pins invisíveis por uma expressão de zoom que o MapLibre rejeitava em silêncio, com os três limpos.

Depois de a Task 12 renderizar o componente numa tela, capture o mapa em 1512 e 390 de largura e **olhe a imagem**.

Confirme, olhando:
- a linha âmbar existe e segue a estrada (ou os segmentos retos, sem traçado)
- o tracejado escuro aparece por cima dela, lendo como faixa central
- a rota não cobre nenhum pin
- nada de `Error: layers[...]` no console

Se a linha não aparecer, suspeite primeiro de expressão inválida em `line-width` ou `line-dasharray`: o MapLibre descarta a camada inteira sem lançar.

- [ ] **Step 5: Escrever no CHANGELOG e commitar**

Sob `### Adicionado`:

```markdown
- A rota de uma viagem é desenhada no mapa como a própria marca do Rastro: asfalto
  âmbar com faixa central tracejada. É o único lugar em que o desenho da marca
  vira informação de verdade
```

```bash
git add src/lib/map/trip-route-layers.ts src/components/map/TripRouteLayer.tsx CHANGELOG.md
git commit -m "feat: a rota no mapa e a marca virando dado"
```

---

# Fase 6 — Telas

### Task 11: Lista de viagens

**Files:**
- Modify: `src/app/(app)/viagens/page.tsx`
- Create: `src/components/trips/TripList.tsx`

**Interfaces:**
- Consumes: `getTripRepository` de `@/lib/data`; `TripSummary` de `@/domain/trip`; `formatDistanceKm`, `formatDurationMinutes` de `@/domain/geo`; `formatVisitDate` de `@/domain/dates`; `TRIP_STATUS_LABELS` de `@/domain/trip`; `OverlayPanel`.
- Produces: componente `TripList`.

- [ ] **Step 1: Escrever a lista**

Crie `src/components/trips/TripList.tsx`. Sem card, sem glifo, sem sombra: linhas separadas por hairline, número em `.instrument-value`.

```tsx
import Link from 'next/link'
import { formatVisitDate } from '@/domain/dates'
import { formatDistanceKm, formatDurationMinutes } from '@/domain/geo'
import { TRIP_STATUS_LABELS, type TripSummary } from '@/domain/trip'

interface TripListProps {
  trips: TripSummary[]
}

export function TripList({ trips }: TripListProps) {
  if (trips.length === 0) {
    return (
      <div className="flex flex-1 flex-col justify-center gap-3 px-5">
        <span className="instrument-label">Viagens</span>
        <p className="text-body leading-relaxed text-ink-muted">
          Nenhuma viagem ainda. Monte um roteiro e ele aparece aqui.
        </p>
        <Link
          href="/viagens/nova"
          className="press mt-2 flex h-11 items-center justify-center rounded-md
                     bg-accent px-4 text-small font-bold text-on-accent
                     hover:bg-accent-strong"
        >
          Montar um roteiro
        </Link>
      </div>
    )
  }

  return (
    <ul className="flex-1 overflow-y-auto">
      {trips.map((trip) => (
        <li key={trip.id} className="border-b border-line last:border-b-0">
          <Link
            href={`/viagens/${trip.slug}`}
            className="flex flex-col gap-1 px-5 py-4 hover:bg-overlay"
          >
            <span className="flex items-baseline justify-between gap-3">
              <span className="text-body text-ink">{trip.title}</span>
              {trip.distanceKm !== null ? (
                <span className="instrument-value text-small whitespace-nowrap text-ink-faint">
                  {formatDistanceKm(trip.distanceKm)} km
                </span>
              ) : null}
            </span>
            <span className="instrument-label flex items-center gap-2">
              <span>{TRIP_STATUS_LABELS[trip.status]}</span>
              {trip.endedAt ? (
                <span>{formatVisitDate(trip.endedAt.slice(0, 10))}</span>
              ) : null}
              {trip.durationMinutes !== null ? (
                <span>{formatDurationMinutes(trip.durationMinutes)}</span>
              ) : null}
              {/* Sem traçado gravado, o número veio de estimativa. Dizer isso é
                  a mesma regra que impede apresentar dado de mocks como real. */}
              {!trip.hasRealRoute ? <span>estimado</span> : null}
            </span>
          </Link>
        </li>
      ))}
    </ul>
  )
}
```

- [ ] **Step 2: Ligar a página**

Substitua `src/app/(app)/viagens/page.tsx`:

```tsx
import { OverlayPanel } from '@/components/layout/OverlayPanel'
import { TripList } from '@/components/trips/TripList'
import { getTripRepository } from '@/lib/data'

export default async function ViagensPage() {
  const repository = await getTripRepository()
  const trips = await repository.listTrips()

  return (
    <OverlayPanel side="right">
      <h1 className="sr-only">Viagens</h1>
      <TripList trips={trips} />
    </OverlayPanel>
  )
}
```

- [ ] **Step 3: Rodar lint e typecheck**

Run: `npm run lint && npm run typecheck`
Expected: limpos.

- [ ] **Step 4: Ver a tela**

Suba `npm run dev` e abra `/viagens` autenticado.
Expected: o estado vazio com o botão âmbar. Nenhum erro no console.

- [ ] **Step 5: Escrever no CHANGELOG e commitar**

Sob `### Adicionado`:

```markdown
- Tela de Viagens deixa de ser um aviso de "em breve" e passa a listar as suas
  viagens, com quilometragem e duração — e dizendo quando o número é estimativa
```

```bash
git add "src/app/(app)/viagens/page.tsx" src/components/trips/TripList.tsx CHANGELOG.md
git commit -m "feat: a tela de viagens deixa de ser um aviso"
```

---

### Task 12: Proposta, roteiro e conclusão

**Files:**
- Create: `src/app/(app)/viagens/nova/page.tsx`
- Create: `src/components/trips/TripProposalForm.tsx`
- Create: `src/app/(app)/viagens/[slug]/page.tsx`
- Create: `src/components/trips/TripItineraryView.tsx`
- Create: `src/app/(app)/viagens/[slug]/concluir/page.tsx`
- Create: `src/components/trips/TripCompletionForm.tsx`

**Interfaces:**
- Consumes: `proposeTripAction`, `createTripAction`, `completeTripAction` de `@/app/actions/trip-actions`; `TIME_BUDGET_LABELS`, `TIME_BUDGET_MINUTES` de `@/domain/discovery`; `CATEGORY_LABELS`, `PLACE_CATEGORIES` de `@/domain/place`; `googleMapsRouteUrl` de `@/domain/google-maps-link`; `TripRouteLayer`; `InlineMessage`, `Button`, `Chip`, `Field`, `Toggle` de `@/components/ui/*`.
- Produces: as três telas.

Como esta tarefa monta três telas que compartilham a mesma gramática, siga a ordem: proposta → roteiro → conclusão, commitando cada uma. **Antes de escrever cada formulário, leia `src/components/explore/DiscoveryView.tsx`** — ele já resolve exatamente este formato (orçamento de tempo, categorias, recusa com sugestão) e a nova tela deve parecer irmã dele, não prima distante.

- [ ] **Step 1: Ler o padrão existente**

Run: leia `src/components/explore/DiscoveryView.tsx` inteiro, e `src/components/ui/InlineMessage.tsx`, `Button.tsx`, `Chip.tsx`, `Toggle.tsx`.

Anote as props reais de cada um. Não invente `variant` nem `size` que não existam.

- [ ] **Step 2: Escrever a tela de proposta**

`src/components/trips/TripProposalForm.tsx`, cliente, com `useTransition`. Estrutura obrigatória:

- Um `Chip` por `TimeBudget`, rotulado por `TIME_BUDGET_LABELS`, com `aria-pressed`.
- Um `Chip` por categoria de `PLACE_CATEGORIES`, rotulado por `CATEGORY_LABELS`.
- Campo de raio reaproveitando `RADIUS_OPTIONS_KM` de `@/domain/filters`.
- Botão "Propor roteiro" chamando `proposeTripAction`.
- Em recusa, `InlineMessage` com a mensagem que a action devolveu — **sem reescrevê-la no cliente**, porque ela vem de `ITINERARY_REFUSAL_MESSAGES` no domínio.
- Em sucesso, a lista de paradas propostas com nome, o total em `.instrument-value`, um campo de título, e "Salvar viagem" chamando `createTripAction`; ao voltar `ok`, `router.push` para `/viagens/<slug>`.
- Cada parada proposta tem um botão "Remover", que apenas tira o id da lista local e **re-chama `createTripAction` só no salvar** — a reordenação acontece no domínio, então remover não precisa recalcular no cliente.

`src/app/(app)/viagens/nova/page.tsx`:

```tsx
import { OverlayPanel } from '@/components/layout/OverlayPanel'
import { TripProposalForm } from '@/components/trips/TripProposalForm'

export default function NovaViagemPage() {
  return (
    <OverlayPanel side="right">
      <h1 className="sr-only">Montar um roteiro</h1>
      <TripProposalForm />
    </OverlayPanel>
  )
}
```

- [ ] **Step 3: Rodar lint e typecheck, e ver a tela**

Run: `npm run lint && npm run typecheck`
Abra `/viagens/nova`, proponha um roteiro com "dia inteiro" e sem categoria.
Expected: paradas propostas com total. Depois teste "2 horas" com raio grande e confirme que aparece a mensagem de `budget-too-small`, **não** a de `no-candidates`.

- [ ] **Step 4: Commitar a proposta**

Sob `### Adicionado`:

```markdown
- O Rastro passa a propor uma volta inteira: você diz quanto tempo tem e que tipo
  de lugar quer ver, e ele encadeia as paradas
```

```bash
git add "src/app/(app)/viagens/nova" src/components/trips/TripProposalForm.tsx CHANGELOG.md
git commit -m "feat: o Rastro propoe a volta inteira"
```

- [ ] **Step 5: Escrever a tela de roteiro**

`src/components/trips/TripItineraryView.tsx` recebe `trip: TripDetail` e mostra:

- Título, status, total de km e duração em `.instrument-value`.
- Se `!trip.hasRealRoute`, uma linha dizendo que os números são estimados.
- As paradas em ordem, separadas por hairline, cada uma com o número da posição em mono.
- Link "Abrir no Google Maps" usando `googleMapsRouteUrl(trip.originCoordinates, trip.stops.map(s => s.coordinates))`, com `target="_blank"` e `rel="noopener noreferrer"`.
- Se `trip.status === 'planned'`, link para `/viagens/<slug>/concluir`.

`src/app/(app)/viagens/[slug]/page.tsx` busca por `getBySlug`, chama `notFound()` quando `null`, renderiza `<TripRouteLayer trip={trip} />` e o `TripItineraryView` dentro de um `OverlayPanel`.

- [ ] **Step 6: VERIFICAÇÃO VISUAL do traçado**

Este é o passo que fecha a Task 10.

Salve uma viagem, abra `/viagens/<slug>`, e capture o mapa em 1512 e 390. **Olhe as duas imagens.** Confirme os quatro itens da Task 10 Step 4.

Não marque esta tarefa como pronta sem ter visto a linha desenhada.

- [ ] **Step 7: Commitar o roteiro**

Sob `### Adicionado`:

```markdown
- A tela de uma viagem mostra as paradas na ordem, o traçado no mapa, e abre o
  roteiro no Google Maps num toque
```

```bash
git add "src/app/(app)/viagens/[slug]/page.tsx" src/components/trips/TripItineraryView.tsx CHANGELOG.md
git commit -m "feat: a tela do roteiro, com o tracado no mapa"
```

- [ ] **Step 8: Escrever a tela de conclusão**

`src/components/trips/TripCompletionForm.tsx`. Este vem com código porque é onde
uma escolha silenciosa faria o produto mentir:

```tsx
'use client'

import { useState, useTransition } from 'react'
import { useRouter } from 'next/navigation'
import { completeTripAction } from '@/app/actions/trip-actions'
import { InlineMessage } from '@/components/ui/InlineMessage'
import type { TripDetail } from '@/domain/trip'

interface TripCompletionFormProps {
  trip: TripDetail
}

export function TripCompletionForm({ trip }: TripCompletionFormProps) {
  const router = useRouter()
  const [pending, startTransition] = useTransition()
  const [error, setError] = useState<string | null>(null)

  // Parada sem `placeId` não entra: combustível e almoço não são lugares do
  // catálogo e não viram memória no mapa.
  const catalogStops = trip.stops.filter((stop) => stop.placeId !== null)

  // Todas marcadas por padrão: o caso comum é a viagem ter acontecido como
  // planejada, e o trabalho do usuário é apontar a exceção, não confirmar o óbvio.
  const [confirmed, setConfirmed] = useState<string[]>(
    catalogStops.map((stop) => stop.id),
  )

  function toggle(id: string) {
    setConfirmed((current) =>
      current.includes(id)
        ? current.filter((item) => item !== id)
        : [...current, id],
    )
  }

  function submit() {
    setError(null)
    startTransition(async () => {
      const result = await completeTripAction(trip.id, confirmed)
      if (!result.ok) {
        setError(result.message)
        return
      }
      router.push(`/viagens/${trip.slug}`)
    })
  }

  return (
    <div className="flex flex-1 flex-col overflow-y-auto">
      <p className="border-b border-line px-5 py-4 text-body leading-relaxed text-ink-muted">
        Desmarque o que não aconteceu. Só o que ficar marcado vira visita
        registrada no mapa.
      </p>

      <ul>
        {catalogStops.map((stop) => (
          <li key={stop.id} className="border-b border-line">
            <label className="flex items-center gap-3 px-5 py-4">
              <input
                type="checkbox"
                checked={confirmed.includes(stop.id)}
                onChange={() => toggle(stop.id)}
                className="size-5 accent-accent"
              />
              <span className="text-body text-ink">{stop.label}</span>
            </label>
          </li>
        ))}
      </ul>

      {error ? (
        <div className="px-5 py-4">
          <InlineMessage tone="alert">{error}</InlineMessage>
        </div>
      ) : null}

      {/*
        NÃO desabilite por lista vazia. Você rodou e tudo estava fechado — isso é
        um fato, e o produto não vai discutir com ele. Uma viagem concluída sem
        nenhuma visita é informação válida.
      */}
      <div className="mt-auto border-t border-line px-5 py-4">
        <button
          type="button"
          onClick={submit}
          disabled={pending}
          className="press flex h-11 w-full items-center justify-center rounded-md
                     bg-accent px-4 text-small font-bold text-on-accent
                     hover:bg-accent-strong disabled:opacity-(--disabled-opacity)"
        >
          {pending ? 'Concluindo…' : 'Concluir viagem'}
        </button>
      </div>
    </div>
  )
}
```

**Confira a prop real do `InlineMessage`** antes de usar `tone="alert"` — se o
componente usar outro nome, ajuste ao existente em vez de mudar o componente.

`src/app/(app)/viagens/[slug]/concluir/page.tsx` busca por `getBySlug`, `notFound()` quando `null`, e renderiza o formulário no `OverlayPanel`.

- [ ] **Step 9: Verificar o ciclo fechado de ponta a ponta**

Este é o passo que prova a premissa da funcionalidade.

1. Anote a cor de um pin de um lugar não visitado no mapa.
2. Monte e salve uma viagem que inclua esse lugar.
3. Conclua a viagem com aquela parada marcada.
4. Volte ao mapa em `/`.

Expected: o pin daquele lugar agora é **verde** (`--color-visited`, `#8fa36a`), e o painel do lugar mostra a visita registrada com a data de hoje.

Depois repita concluindo **outra** viagem com uma parada **desmarcada**: aquele pin **não** deve mudar de cor. É essa a diferença entre memória e intenção.

- [ ] **Step 10: Commitar a conclusão**

Sob `### Adicionado`:

```markdown
- Ao concluir uma viagem você confirma quais paradas realmente aconteceram, e só
  essas viram visita registrada — o pin muda de cor sozinho no mapa. O que você
  desmarcou continua sendo só roteiro, porque o mapa não deve afirmar que você
  esteve onde não esteve
```

```bash
git add "src/app/(app)/viagens/[slug]/concluir" src/components/trips/TripCompletionForm.tsx CHANGELOG.md
git commit -m "feat: concluir a viagem pinta o mapa, e so o que aconteceu"
```

---

### Task 13: As outras duas entradas — fixar o destino, e montar no dedo

A spec §3.4 fecha **três** entradas, e as tarefas 11 e 12 só entregam a primeira.
Esta tarefa entrega as outras duas. Sem ela a funcionalidade está incompleta em
relação ao que foi aprovado.

**Files:**
- Modify: `src/components/trips/TripProposalForm.tsx`
- Modify: `src/app/actions/trip-actions.ts`
- Modify: `src/components/explore/PlaceStateControls.tsx` (ou o painel de lugar equivalente)

**Interfaces:**
- Consumes: `proposeTripAction`, `createTripAction` (Task 9); `anchorPlaceId` de `ItineraryRequest` (Task 3); `ExplorePlace` de `@/domain/place`.
- Produces: nenhuma assinatura nova. `proposeTripAction` passa a receber `anchorPlaceId` preenchido, e `createTripAction` passa a ser alcançável com uma lista montada à mão.

- [ ] **Step 1: Entrada 2 — fixar o destino no formulário de proposta**

Em `TripProposalForm.tsx`, acrescente um seletor de destino opcional acima do
orçamento de tempo:

```tsx
{/* Entrada 2 da spec: você já sabe onde quer chegar, e o Rastro sugere o que
    parar no caminho. É o mesmo motor — só a âncora muda. */}
<label className="flex flex-col gap-2 px-5 py-4 border-b border-line">
  <span className="instrument-label">Destino, se você já sabe</span>
  <select
    value={anchorPlaceId ?? ''}
    onChange={(event) => setAnchorPlaceId(event.target.value || null)}
    className="h-11 rounded-md border border-line-strong bg-raised px-3
               text-body text-ink"
  >
    <option value="">Qualquer um — me proponha a volta</option>
    {places.map((place) => (
      <option key={place.id} value={place.id}>
        {place.name}
      </option>
    ))}
  </select>
</label>
```

O componente passa a receber `places: ExplorePlace[]` da page (server component),
que já as busca por `getPlaceRepository().listExplorePlaces()`. **Não** busque
lugares no cliente.

`anchorPlaceId` entra no objeto passado a `proposeTripAction` — a action e o
domínio já o suportam desde a Task 3, sem mudança.

- [ ] **Step 2: Verificar a entrada 2**

Abra `/viagens/nova`, escolha um destino distante e "2 horas".
Expected: a mensagem de `anchor-does-not-fit`, **nomeando o problema como tempo** —
não a de `no-candidates`.

Escolha o mesmo destino com "dia inteiro".
Expected: o roteiro proposto **contém** aquele destino, mesmo que ele pontue menos
que outros candidatos.

- [ ] **Step 3: Entrada 3 — adicionar lugar a um roteiro a partir do painel do lugar**

No painel de um lugar (`PlaceStateControls.tsx` ou equivalente), acrescente uma
ação "Adicionar a um roteiro". Ela não cria viagem: guarda o id numa lista e
navega para `/viagens/nova?paradas=<id>,<id>`.

Em `TripProposalForm.tsx`, leia `searchParams.paradas` como lista inicial de
paradas escolhidas à mão. Quando ela vier preenchida:

- **não** chame `proposeTripAction` automaticamente
- mostre as paradas escolhidas com um botão "Remover" em cada
- mostre o total, obtido chamando `proposeTripAction`? **Não.** Adicione ao
  `trip-actions.ts` uma action dedicada:

```ts
/**
 * Ordena e mede um conjunto de paradas escolhido à mão, sem gravar nada.
 *
 * Existe separada de `proposeTripAction` porque aqui NÃO há seleção: o usuário já
 * escolheu, e o único trabalho é ordenar e medir. Reaproveitar a outra faria a
 * pontuação de interesse descartar parada que a pessoa pediu explicitamente.
 */
export async function measureTripAction(placeIds: string[]): Promise<
  | { ok: true; stops: { id: string; name: string }[]; roadKm: number; minutes: number }
  | { ok: false; message: string }
> {
  const profileRepository = await getProfileRepository()
  const profile = await profileRepository.getProfile()
  if (!profile?.originCoordinates) {
    return { ok: false, message: 'Defina sua origem no perfil.' }
  }

  const placeRepository = await getPlaceRepository()
  const all = await placeRepository.listExplorePlaces()
  const chosen = all.filter((place) => placeIds.includes(place.id))

  if (chosen.length === 0) {
    return { ok: false, message: 'Nenhuma das paradas escolhidas existe.' }
  }

  const itinerary = orderAndMeasure(profile.originCoordinates, chosen)

  return {
    ok: true,
    stops: itinerary.stops.map((place) => ({ id: place.id, name: place.name })),
    roadKm: itinerary.totalRoadKm,
    minutes: itinerary.totalRidingMinutes,
  }
}
```

O botão de salvar é o mesmo `createTripAction` das outras duas entradas.

- [ ] **Step 4: Verificar a entrada 3**

Abra o painel de três lugares diferentes e adicione cada um a um roteiro. Vá para
`/viagens/nova`.
Expected: as três paradas listadas, **reordenadas** pelo domínio (não na ordem em
que você clicou), com o total medido. Salvar cria a viagem.

Confirme também que **nenhum** dos três foi descartado: a montagem manual não
aplica pontuação de interesse.

- [ ] **Step 5: Rodar lint, typecheck e a suíte**

Run: `npm run lint && npm run typecheck && npm test -- --run`
Expected: limpos.

- [ ] **Step 6: Escrever no CHANGELOG e commitar**

Sob `### Adicionado`:

```markdown
- Há três jeitos de montar uma viagem: deixar o Rastro propor a volta inteira,
  dizer o destino e receber sugestões do que parar no caminho, ou escolher as
  paradas você mesmo a partir do painel de cada lugar. Em todos, a ordem é
  calculada
```

```bash
git add src/components/trips/TripProposalForm.tsx src/app/actions/trip-actions.ts src/components/explore CHANGELOG.md
git commit -m "feat: as tres entradas de montagem, um motor so"
```

---

## Encerramento

- [ ] **Rodar tudo**

Run: `npm run lint && npm run typecheck && npm test -- --run`
Expected: limpos, e ≥ 139 testes passando.

- [ ] **Rodar a prova de RLS de novo**

Run: `node scripts/verificar-complete-trip-rls.mjs <uuid-de-uma-viagem-real>`
Expected: `OK:` nas duas linhas.

- [ ] **Confirmar que nenhuma dependência entrou**

Run: `git diff --stat main...HEAD -- package.json package-lock.json`
Expected: sem saída.

- [ ] **Atualizar a documentação viva**

Em `docs/DESIGN-SYSTEM.md`, na seção de regras de forma, registre que a rota da viagem é a exceção onde o tracejado da marca aparece fora do logo.

Em `docs/ARCHITECTURE.md`, acrescente `src/lib/routing/` à descrição de camadas.

Commit: `docs: registra a camada de roteamento e o tracejado da rota`

---

## Rastreio da spec

| Requisito da spec | Tarefa |
|---|---|
| §4.1 Pontuação de interesse com recência | 1 |
| §4.2 Ordenação 2-opt | 2 |
| §4.3 Laço de poda, âncora nunca descartada | 3 |
| §4.4 `MINUTES_PER_STOP` e a divergência declarada | 1 (constante), 3 (uso) |
| §4.5 Tipos e as três recusas | 2, 3 |
| §3.1 Link de waypoints para o Google | 4 |
| §7 Fronteira do ORS, `null` como única falha | 5 |
| §3.2 ADR da dependência | 5 |
| §5 Regra de honestidade (`route_geojson is null`) | 6 (`hasRealRoute`), 10, 11 |
| §8 Armadilha da data | 6 |
| §6 Camadas e repositório sem `userId` | 7 |
| §8 Migration `security invoker` | 8 |
| §11 Prova de RLS da `complete_trip` | 8 |
| §6 Server Actions orquestrando | 9 |
| §9.1 Rota desenhada como a marca | 10 |
| §9.2 Ordem calculada, não arrastada | 2, 12, 13 |
| §9.3 As quatro telas | 11, 12 |
| §10 Erros no painel, nunca toast | 9 (mensagens), 12 (`InlineMessage`) |
| §3.3 Confirmação de paradas ao concluir | 8, 12 |
| §3.4 entrada 1 — propor a volta inteira | 12 |
| §3.4 entrada 2 — fixar o destino (âncora) | **13** |
| §3.4 entrada 3 — montar no dedo | **13** |
| §11 Verificação visual obrigatória | 10, 12 |

> **Fraqueza conhecida do plano.** As tarefas 12 e 13 especificam os formulários
> por contrato e por referência a `DiscoveryView.tsx`, em vez de trazerem o JSX
> inteiro como as tarefas de domínio trazem. É deliberado — copiar três
> formulários completos aqui duplicaria decisões que já estão resolvidas naquele
> arquivo, e o risco de divergir dele é maior que o de interpretar o contrato. Mas
> é a parte do plano em que um implementador tem mais liberdade, e portanto onde a
> revisão entre tarefas importa mais. **Leia `DiscoveryView.tsx` antes de escrever
> qualquer formulário.**
