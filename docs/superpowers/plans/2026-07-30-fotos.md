# Fotos — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Anexar fotografias a um lugar, ver a galeria no painel dele, apagar, e fazer `photo_count` parar de mentir — o que acende sozinho a marca de foto no mapa.

**Architecture:** O navegador lê o EXIF do arquivo original, encolhe no canvas, e sobe **direto** para o Supabase Storage num bucket privado, autorizado por política de RLS do próprio Storage. Só então uma Server Action grava a linha. A leitura gera URL assinada em lote, no servidor. Um trigger mantém `photo_count`, e é ele que acende a camada `photoDot` que já existe no mapa.

**Tech Stack:** Next.js 16 (App Router, Server Actions), React 19, TypeScript strict com `noUncheckedIndexedAccess`, Tailwind CSS v4, Supabase (`@supabase/supabase-js`, `@supabase/ssr`) e Supabase Storage, MapLibre GL v5, Vitest, Playwright.

**Spec:** `docs/superpowers/specs/2026-07-30-fotos-design.md`

## Global Constraints

Valem para **toda** tarefa. Não são repetidas em cada uma.

- **`CHANGELOG.md` em todo commit.** Sem exceção para "só documentação". Categorias em PT-BR: `Adicionado`, `Alterado`, `Corrigido`, `Removido`, `Segurança`. Escreva do ponto de vista de quem usa o produto.
- **Ordem do fluxo:** alterar → `npm run lint` → `npm run typecheck` → escrever no `CHANGELOG.md` → `git add` incluindo o CHANGELOG → `git commit`.
- **`npm run lint` roda com `--max-warnings=0`.** Warning é erro.
- **Nenhum `any` novo, nenhum `@ts-ignore` novo, nenhum `console.log` esquecido.**
- **Camadas, verificáveis lendo imports:** `src/domain/` não importa nada do projeto além de `domain` — nem React, nem Next. `src/lib/images/` importa só `domain`. `src/lib/supabase/` não importa nada do projeto. `src/lib/data/` importa `domain` e `lib/supabase`. `src/app/actions/` importa `domain` e `lib`. `src/components/` importa `domain` e `lib`.
- **Repositórios nunca filtram por `user_id`.** A RLS filtra por `auth.uid()` — ADR 0008. `userId` só entra como *valor gravado*.
- **Server Actions são a fronteira de escrita.** Validam com funções puras do domínio, chamam repositórios, revalidam. Nenhuma regra de negócio nelas.
- **O cliente Supabase de navegador é usado APENAS para `storage.from('fotos')`.** Qualquer leitura ou escrita de tabela continua em Server Action. Ver ADR 0014.
- **A chave `service_role` não entra no repositório.** Só a `anon`.
- **Direção visual (ADR 0009, 0010, 0012) — proibido:** card com conteúdo primário, gradiente, glassmorphism decorativo, ícone sem função, sombra difusa, **toast, tooltip só de hover, skeleton, modal**. Erro aparece no painel via `InlineMessage`, cujos tons são `'error' | 'warn' | 'info' | 'ok'`.
- **Glifo é proibido fora da navegação principal** (ADR 0011).
- **Obrigatório:** hairline de 1px, raio na escala (`sm` 8 / `md` 14 / `lg` 20 / `xl` 28 / `2xl` 36), dado numérico em `.instrument-value`, piso de corpo 17px (`text-body` — **`text-base` não existe e não gera CSS**).
- **Textos de interface em PT-BR; código em inglês.**
- **Comentário explica o *porquê*, nunca o *quê*.**
- **`lint`, `typecheck` e os testes não desenham a tela.** Toda tarefa visual exige captura antes de ser declarada pronta.

---

## File Structure

| Arquivo | Responsabilidade |
|---|---|
| `supabase/migrations/0004_photos.sql` | Renomeia a tabela, corrige as constraints, cria o trigger |
| `supabase/migrations/0005_photos_storage.sql` | Bucket privado e políticas de Storage |
| `scripts/verificar-storage-rls.mjs` | Prova de RLS do Storage, só com chave anon |
| `src/domain/photo.ts` | Tipos, validação, formatos aceitos. Puro |
| `src/domain/photo.test.ts` | Testes do acima |
| `src/lib/images/exif.ts` | Leitor de EXIF escrito à mão. Puro |
| `src/lib/images/exif.test.ts` | Testes, com o JPEG montado no próprio teste |
| `src/lib/images/resize.ts` | `targetDimensions` puro + a casca de canvas |
| `src/lib/images/resize.test.ts` | Testes de `targetDimensions` |
| `src/lib/images/storage-path.ts` | Monta `{userId}/{placeId}/{uuid}.jpg`. Puro |
| `src/lib/images/storage-path.test.ts` | Testes |
| `src/lib/supabase/browser.ts` | Primeiro cliente de navegador do projeto |
| `src/lib/data/photo-repository.ts` | Contrato |
| `src/lib/data/supabase/photo-row.ts` | Tradução linha↔domínio |
| `src/lib/data/supabase/photo-row.test.ts` | Testes |
| `src/lib/data/supabase/supabase-photo-repository.ts` | Adapter, e quem assina as URLs |
| `src/app/actions/photo-actions.ts` | Fronteira de escrita |
| `src/components/explore/PlacePhotos.tsx` | Galeria e upload |
| `docs/decisions/0014-cliente-supabase-no-navegador.md` | ADR |

---

### Task 1: Migration 0004 — o esquema e o trigger

**Files:**
- Create: `supabase/migrations/0004_photos.sql`

**Interfaces:**
- Consumes: tabelas `trip_photos`, `place_user_states`, `places` da migration 0001.
- Produces: tabela `photos` e o trigger `photos_refresh_count`, consumidos pelas tarefas 7 e 8.

- [ ] **Step 1: Escrever a migration**

```sql
-- Fotos passam a pertencer ao LUGAR, com a viagem opcional.
--
-- O esquema original se contradizia: place_visits.trip_id aceita nulo e
-- trip_photos.trip_id não. O produto deixava registrar "estive aqui" sem viagem,
-- mas não anexar a foto tirada lá sem inventar uma — e isso excluiria toda
-- fotografia anterior ao próprio app.

alter table trip_photos rename to photos;

alter table photos alter column trip_id  drop not null;
alter table photos alter column place_id set not null;

-- OBRIGATÓRIO junto com o `set not null` acima. `place_id` era
-- `on delete set null`, e as duas coisas se contradizem: apagar um lugar
-- tentaria escrever nulo numa coluna que não aceita nulo, e a exclusão falharia.
--
-- `cascade` é a mesma escolha de place_visits.place_id: se o lugar deixa de
-- existir, a memória dele vai junto.
alter table photos drop constraint trip_photos_place_id_fkey;
alter table photos add  constraint photos_place_id_fkey
  foreign key (place_id) references places on delete cascade;

-- Sempre conhecidas: saem do canvas no próprio upload. Nulas quebrariam a
-- reserva de proporção da grade, que é o que substitui o skeleton proibido.
alter table photos alter column width  set not null;
alter table photos alter column height set not null;

-- Data civil, não instante: a memória é do dia. Mesma escolha de
-- place_visits.visited_at.
alter table photos rename column taken_at to taken_on;
alter table photos alter column taken_on type date using taken_on::date;
-- Continua aceitando nulo DE PROPÓSITO: nulo é "não sabemos quando foi tirada".
-- Preenchê-la com o dia do upload seria inventar memória.

-- Coluna que nunca seria escrita: extraímos GPS e data para colunas tipadas, e o
-- bloco bruto carrega número de série do aparelho e mais dado pessoal do que
-- precisamos guardar. Manter coluna que ninguém preenche é a armadilha que já
-- custou o photo_count.
alter table photos drop column exif;

alter index trip_photos_trip_idx    rename to photos_trip_idx;
alter index trip_photos_place_idx   rename to photos_place_idx;
alter index trip_photos_located_idx rename to photos_located_idx;

alter policy trip_photos_own on photos rename to photos_own;

-- ------------------------------------------------- derivação de fotos

-- Mantém place_user_states.photo_count a partir de photos. Espelha
-- refresh_place_user_state(), que faz o mesmo para visitas.
--
-- `security definer` aqui, e `security invoker` na complete_trip da 0003: a
-- diferença é deliberada. A complete_trip é chamada PELO usuário e a RLS precisa
-- decidir se ele pode; um trigger roda como efeito de uma escrita que a RLS já
-- autorizou, e só mantém cache derivado.
create or replace function refresh_place_photo_count()
returns trigger
language plpgsql
security definer
set search_path = public
as $$
declare
  target_user  uuid := coalesce(new.user_id, old.user_id);
  target_place uuid := coalesce(new.place_id, old.place_id);
begin
  insert into place_user_states (user_id, place_id)
  values (target_user, target_place)
  on conflict (user_id, place_id) do nothing;

  update place_user_states s
     set photo_count = (
           select count(*) from photos
            where user_id = target_user and place_id = target_place
         ),
         updated_at = now()
   where s.user_id = target_user and s.place_id = target_place;

  return null;
end;
$$;

create trigger photos_refresh_count
after insert or update or delete on photos
for each row execute function refresh_place_photo_count();
```

- [ ] **Step 2: Confirmar o nome da constraint ANTES de rodar**

A migration derruba `trip_photos_place_id_fkey` pelo nome. O Postgres nomeia
constraint de chave estrangeira como `<tabela>_<coluna>_fkey`, e **renomear a
tabela não renomeia as constraints** — por isso o nome antigo é o correto. Mas se
ele divergir, a migration falha no meio, com parte já aplicada.

No SQL Editor:

```sql
select conname from pg_constraint
 where conrelid = 'trip_photos'::regclass and contype = 'f';
```

Expected: a lista inclui `trip_photos_place_id_fkey`. Se o nome for outro, ajuste
a linha `drop constraint` antes de seguir.

- [ ] **Step 3: Aplicar no Supabase**

Cole o arquivo inteiro no SQL Editor e execute.
Expected: `Success. No rows returned`.

- [ ] **Step 4: Conferir que a tabela ficou como o esperado**

No SQL Editor:

```sql
select column_name, data_type, is_nullable
  from information_schema.columns
 where table_name = 'photos'
 order by ordinal_position;
```

Expected: `trip_id` com `is_nullable = YES`; `place_id`, `width`, `height` com `NO`; `taken_on` do tipo `date` e `is_nullable = YES`; **nenhuma coluna `exif`**.

- [ ] **Step 5: Escrever no CHANGELOG e commitar**

Sob `### Alterado`:

```markdown
- Uma fotografia passa a pertencer ao lugar, e a viagem vira opcional. Antes o
  banco exigia uma viagem para cada foto, o que deixaria de fora toda fotografia
  anterior ao aplicativo
```

```bash
git add supabase/migrations/0004_photos.sql CHANGELOG.md
git commit -m "feat: a foto pertence ao lugar, e a viagem e opcional"
```

---

### Task 2: Migration 0005 — o bucket e a prova de RLS do Storage

**Files:**
- Create: `supabase/migrations/0005_photos_storage.sql`
- Create: `scripts/verificar-storage-rls.mjs`
- Modify: `docs/VERIFICACAO-RLS.md`

**Interfaces:**
- Produces: bucket `fotos` e as três políticas sobre `storage.objects`, consumidos pelas tarefas 6 e 9.

- [ ] **Step 1: Escrever a migration**

```sql
-- Bucket privado. Foto de viagem carrega rosto, placa, e onde você estava e
-- quando. Caminho imprevisível NÃO é segurança: link vazado vale para sempre.
insert into storage.buckets (id, name, public)
values ('fotos', 'fotos', false)
on conflict (id) do nothing;

-- Cada usuário preso à própria pasta. O caminho é {userId}/{placeId}/{uuid}.jpg,
-- então o primeiro segmento é a identidade — e é ela que a política compara.
create policy fotos_select_own on storage.objects
  for select using (
    bucket_id = 'fotos' and (storage.foldername(name))[1] = auth.uid()::text
  );

create policy fotos_insert_own on storage.objects
  for insert with check (
    bucket_id = 'fotos' and (storage.foldername(name))[1] = auth.uid()::text
  );

create policy fotos_delete_own on storage.objects
  for delete using (
    bucket_id = 'fotos' and (storage.foldername(name))[1] = auth.uid()::text
  );
```

- [ ] **Step 2: Aplicar no Supabase**

Cole no SQL Editor e execute.
Expected: `Success. No rows returned`.

- [ ] **Step 3: Escrever a prova de RLS**

Crie `scripts/verificar-storage-rls.mjs`. Usa **apenas** a chave `anon`.

```js
/**
 * Prova que o bucket `fotos` é privado e que ninguém lê a pasta de outro.
 *
 * "Confio que a RLS pega" não é verificação. Este script existe pelo mesmo
 * motivo do verificar-complete-trip-rls.mjs.
 *
 * Uso: node scripts/verificar-storage-rls.mjs
 */
import { readFileSync } from 'node:fs'
import { createClient } from '@supabase/supabase-js'

const env = Object.fromEntries(
  readFileSync('.env.local', 'utf8')
    .split('\n')
    .filter((l) => l.includes('=') && !l.trim().startsWith('#'))
    .map((l) => {
      const at = l.indexOf('=')
      return [l.slice(0, at).trim(), l.slice(at + 1).trim()]
    }),
)

const anon = createClient(
  env.NEXT_PUBLIC_SUPABASE_URL,
  env.NEXT_PUBLIC_SUPABASE_ANON_KEY,
)

let falhou = false
const pastaDeOutro = '00000000-0000-0000-0000-000000000000/x/y.jpg'

const { data: lista, error: erroLista } = await anon.storage
  .from('fotos')
  .list('00000000-0000-0000-0000-000000000000')

if (erroLista) {
  console.log('OK: anonimo nao lista pasta de outro ->', erroLista.message)
} else if (lista.length === 0) {
  console.log('OK: anonimo nao ve nada na pasta de outro (RLS filtrou)')
} else {
  console.error('FALHOU: anonimo listou', lista)
  falhou = true
}

const { error: erroUpload } = await anon.storage
  .from('fotos')
  .upload(pastaDeOutro, new Blob(['x'], { type: 'text/plain' }))

if (!erroUpload) {
  console.error('FALHOU: anonimo escreveu na pasta de outro')
  falhou = true
} else {
  console.log('OK: anonimo nao escreve ->', erroUpload.message)
}

// Bucket privado: a URL publica NAO deve servir o objeto.
const { data: publica } = anon.storage.from('fotos').getPublicUrl(pastaDeOutro)
const resposta = await fetch(publica.publicUrl)
if (resposta.ok) {
  console.error('FALHOU: URL publica serviu o objeto. O bucket nao e privado.')
  falhou = true
} else {
  console.log('OK: URL publica recusada ->', resposta.status)
}

process.exit(falhou ? 1 : 0)
```

- [ ] **Step 4: Rodar a prova**

Run: `node scripts/verificar-storage-rls.mjs`
Expected: três linhas começando com `OK:`, saída zero.

Se aparecer `FALHOU`, **pare**: nenhuma outra tarefa deve seguir com o bucket exposto.

- [ ] **Step 5: Registrar em `docs/VERIFICACAO-RLS.md`**

Acrescente antes da seção "Se algum bloco falhar":

```markdown
## Storage `fotos` — verificada

Bucket **privado**. As três políticas sobre `storage.objects` prendem cada usuário
à própria pasta, comparando o primeiro segmento do caminho
(`{userId}/{placeId}/{uuid}.jpg`) com `auth.uid()`.

```
node scripts/verificar-storage-rls.mjs
```

Confirma três coisas com a chave anon: o anônimo não lista a pasta de outro, não
escreve nela, e a URL pública **não serve** o objeto — esta última é a que prova
que o bucket é privado de verdade, e não só de difícil adivinhação.

Rode de novo ao mexer em qualquer política de Storage.

---
```

- [ ] **Step 6: Escrever no CHANGELOG e commitar**

Sob `### Segurança`:

```markdown
- As fotografias ficam em armazenamento privado: cada uma só é acessível por um
  link temporário gerado para você. Verificado que ninguém alcança a pasta de
  outra pessoa, nem por link direto
```

```bash
git add supabase/migrations/0005_photos_storage.sql scripts/verificar-storage-rls.mjs docs/VERIFICACAO-RLS.md CHANGELOG.md
git commit -m "feat: bucket privado de fotos, com a RLS provada"
```

---

### Task 3: Domínio — tipos e validação

**Files:**
- Create: `src/domain/photo.ts`
- Create: `src/domain/photo.test.ts`

**Interfaces:**
- Consumes: `Coordinates` de `src/domain/geo.ts`.
- Produces: `ACCEPTED_IMAGE_TYPES`, `MAX_CAPTION_LENGTH`, `Photo`, `NewPhoto`, `PhotoValidationError`, `PHOTO_VALIDATION_MESSAGES`, `isAcceptedImageType(type: string): boolean`, `validateNewPhoto(input: NewPhoto): PhotoValidationError[]`.

- [ ] **Step 1: Escrever o teste que falha**

```ts
import { describe, expect, it } from 'vitest'
import {
  MAX_CAPTION_LENGTH,
  isAcceptedImageType,
  validateNewPhoto,
} from './photo'
import type { NewPhoto } from './photo'

function photo(overrides: Partial<NewPhoto> = {}): NewPhoto {
  return {
    placeId: 'p1',
    storagePath: 'u1/p1/abc.jpg',
    width: 2000,
    height: 1500,
    coordinates: null,
    takenOn: null,
    caption: null,
    ...overrides,
  }
}

describe('isAcceptedImageType', () => {
  it('aceita os três formatos que o navegador sabe ler e encolher', () => {
    expect(isAcceptedImageType('image/jpeg')).toBe(true)
    expect(isAcceptedImageType('image/png')).toBe(true)
    expect(isAcceptedImageType('image/webp')).toBe(true)
  })

  it('recusa HEIC, que quebra tanto o EXIF quanto o canvas', () => {
    expect(isAcceptedImageType('image/heic')).toBe(false)
    expect(isAcceptedImageType('image/heif')).toBe(false)
  })

  it('recusa qualquer coisa que não seja imagem', () => {
    expect(isAcceptedImageType('application/pdf')).toBe(false)
    expect(isAcceptedImageType('')).toBe(false)
  })
})

describe('validateNewPhoto', () => {
  it('aceita uma foto sem legenda, sem data e sem coordenada', () => {
    // Nulo nos três é o caso comum: foto antiga, sem EXIF, sem legenda.
    expect(validateNewPhoto(photo())).toEqual([])
  })

  it('recusa legenda longa demais', () => {
    const caption = 'x'.repeat(MAX_CAPTION_LENGTH + 1)
    expect(validateNewPhoto(photo({ caption }))).toContain('caption-too-long')
  })

  it('aceita legenda exatamente no limite', () => {
    const caption = 'x'.repeat(MAX_CAPTION_LENGTH)
    expect(validateNewPhoto(photo({ caption }))).toEqual([])
  })

  it('recusa dimensão ausente ou absurda', () => {
    expect(validateNewPhoto(photo({ width: 0 }))).toContain('invalid-dimensions')
    expect(validateNewPhoto(photo({ height: -1 }))).toContain(
      'invalid-dimensions',
    )
  })

  it('recusa caminho de armazenamento vazio', () => {
    expect(validateNewPhoto(photo({ storagePath: '  ' }))).toContain(
      'storage-path-required',
    )
  })
})
```

- [ ] **Step 2: Rodar para confirmar que falha**

Run: `npx vitest run src/domain/photo.test.ts`
Expected: FAIL — `Failed to resolve import "./photo"`

- [ ] **Step 3: Escrever a implementação**

```ts
import type { Coordinates } from './geo'

/**
 * Formatos que o navegador consegue ler EXIF e redimensionar.
 *
 * HEIC fica de fora e isso é limitação declarada, não esquecimento: o leitor de
 * EXIF entende só JPEG, e o canvas do Chrome não decodifica HEIC. Arquivo fora
 * desta lista é recusado com o motivo na tela, nunca engolido em silêncio.
 */
export const ACCEPTED_IMAGE_TYPES = [
  'image/jpeg',
  'image/png',
  'image/webp',
] as const

export const MAX_CAPTION_LENGTH = 280

export function isAcceptedImageType(type: string): boolean {
  return (ACCEPTED_IMAGE_TYPES as readonly string[]).includes(type)
}

export interface Photo {
  id: string
  placeId: string
  /** Nulo é o caso comum: foto anexada a um lugar, fora de qualquer viagem. */
  tripId: string | null
  storagePath: string
  width: number
  height: number
  /** Do EXIF quando houver. Nulo é legítimo, e a interface diz que não sabe. */
  coordinates: Coordinates | null
  /**
   * Data civil `YYYY-MM-DD`, vinda do EXIF. **Nulo significa "não sabemos"**.
   *
   * NÃO cai para o dia do upload: a data em que você mexeu no arquivo não é a
   * data em que a foto foi tirada, e apresentá-la como se fosse seria inventar
   * memória. `created_at` já registra o upload, para quem precisar dele.
   */
  takenOn: string | null
  caption: string | null
  sortIndex: number
}

/** O que o cliente já sabe ao terminar o upload. */
export interface NewPhoto {
  placeId: string
  storagePath: string
  width: number
  height: number
  coordinates: Coordinates | null
  takenOn: string | null
  caption: string | null
}

export type PhotoValidationError =
  | 'caption-too-long'
  | 'invalid-dimensions'
  | 'storage-path-required'

export const PHOTO_VALIDATION_MESSAGES: Record<PhotoValidationError, string> = {
  'caption-too-long': `A legenda passa de ${MAX_CAPTION_LENGTH} caracteres.`,
  'invalid-dimensions': 'Não foi possível ler as dimensões da imagem.',
  'storage-path-required': 'A imagem não chegou ao armazenamento.',
}

export function validateNewPhoto(input: NewPhoto): PhotoValidationError[] {
  const errors: PhotoValidationError[] = []

  if (input.caption && input.caption.length > MAX_CAPTION_LENGTH) {
    errors.push('caption-too-long')
  }
  if (
    !Number.isInteger(input.width) ||
    !Number.isInteger(input.height) ||
    input.width <= 0 ||
    input.height <= 0
  ) {
    errors.push('invalid-dimensions')
  }
  if (input.storagePath.trim().length === 0) {
    errors.push('storage-path-required')
  }

  return errors
}
```

- [ ] **Step 4: Rodar para confirmar que passa**

Run: `npx vitest run src/domain/photo.test.ts`
Expected: PASS — 8 testes.

- [ ] **Step 5: Lint, typecheck, CHANGELOG e commit**

Run: `npm run lint && npm run typecheck`

Sob `### Adicionado`:

```markdown
- Legenda de fotografia aceita até 280 caracteres, e arquivo em formato que o
  navegador não sabe processar é recusado dizendo qual mandar
```

```bash
git add src/domain/photo.ts src/domain/photo.test.ts CHANGELOG.md
git commit -m "feat: dominio de fotos, com HEIC recusado de forma explicita"
```

---

### Task 4: Leitor de EXIF

**Files:**
- Create: `src/lib/images/exif.ts`
- Create: `src/lib/images/exif.test.ts`

**Interfaces:**
- Consumes: `Coordinates` de `src/domain/geo.ts`.
- Produces: `ExifData { coordinates: Coordinates | null; takenOn: string | null }`, `readExif(buffer: ArrayBuffer): ExifData`.

`readExif` **nunca lança**: arquivo sem EXIF, truncado ou corrompido devolve `{ coordinates: null, takenOn: null }`.

- [ ] **Step 1: Escrever o teste que falha**

O JPEG é montado no próprio teste. Fixture binário no repositório seria opaco — ninguém saberia dizer o que ele contém sem abrir um editor hexadecimal.

```ts
import { describe, expect, it } from 'vitest'
import { readExif } from './exif'

/**
 * Monta um JPEG mínimo com um bloco EXIF: SOI + APP1(Exif/TIFF) + EOI.
 *
 * `big` escolhe a ordem de bytes do TIFF (`MM` grande, `II` pequena), que é a
 * armadilha central do formato: os dois existem no mundo real e um leitor que
 * assume uma das duas erra metade das fotos.
 */
function buildJpegWithExif(options: {
  big: boolean
  lat?: [number, number, number]
  latRef?: string
  lng?: [number, number, number]
  lngRef?: string
  taken?: string
}): ArrayBuffer {
  const { big } = options
  const body: number[] = []
  const w16 = (v: number) =>
    big ? body.push(v >> 8, v & 0xff) : body.push(v & 0xff, v >> 8)
  const w32 = (v: number) =>
    big
      ? body.push((v >>> 24) & 0xff, (v >>> 16) & 0xff, (v >>> 8) & 0xff, v & 0xff)
      : body.push(v & 0xff, (v >>> 8) & 0xff, (v >>> 16) & 0xff, (v >>> 24) & 0xff)

  // Cabeçalho TIFF: ordem de bytes, o número mágico 42, e o offset do IFD0.
  body.push(big ? 0x4d : 0x49, big ? 0x4d : 0x49)
  w16(42)
  w32(8)

  const entries: { tag: number; type: number; count: number; value: number }[] = []
  const extra: number[] = []
  // Os dados que não cabem em 4 bytes vivem depois dos dois IFDs.
  let extraBase = 0

  const gpsEntries: typeof entries = []
  const rational = (r: [number, number, number]) => {
    const offset = extraBase + extra.length
    for (const n of r) {
      // Cada RATIONAL é numerador/denominador; usamos denominador 1000 para
      // caber minuto e segundo fracionários sem perder precisão.
      const num = Math.round(n * 1000)
      big
        ? extra.push((num >>> 24) & 0xff, (num >>> 16) & 0xff, (num >>> 8) & 0xff, num & 0xff)
        : extra.push(num & 0xff, (num >>> 8) & 0xff, (num >>> 16) & 0xff, (num >>> 24) & 0xff)
      big ? extra.push(0, 0, 0x03, 0xe8) : extra.push(0xe8, 0x03, 0, 0)
    }
    return offset
  }

  const ascii = (s: string) => {
    const offset = extraBase + extra.length
    for (const ch of s) extra.push(ch.charCodeAt(0))
    extra.push(0)
    return offset
  }

  // Layout: IFD0 (2 entradas) + GPS IFD (4 entradas) + Exif IFD (1 entrada).
  const ifd0Size = 2 + 2 * 12 + 4
  const gpsSize = 2 + 4 * 12 + 4
  const exifSize = 2 + 1 * 12 + 4
  const gpsOffset = 8 + ifd0Size
  const exifOffset = gpsOffset + gpsSize
  extraBase = exifOffset + exifSize

  if (options.lat) {
    gpsEntries.push({ tag: 0x0001, type: 2, count: 2, value: 0 })
    gpsEntries.push({ tag: 0x0002, type: 5, count: 3, value: rational(options.lat) })
    gpsEntries.push({ tag: 0x0003, type: 2, count: 2, value: 0 })
    gpsEntries.push({ tag: 0x0004, type: 5, count: 3, value: rational(options.lng!) })
  }
  const takenOffset = options.taken ? ascii(options.taken) : 0

  // IFD0: só os dois ponteiros.
  w16(2)
  const writeEntry = (tag: number, type: number, count: number, value: number, inlineAscii?: string) => {
    w16(tag)
    w16(type)
    w32(count)
    if (inlineAscii) {
      body.push(inlineAscii.charCodeAt(0), 0, 0, 0)
    } else {
      w32(value)
    }
  }
  writeEntry(0x8825, 4, 1, gpsOffset)
  writeEntry(0x8769, 4, 1, exifOffset)
  w32(0)

  // GPS IFD.
  w16(gpsEntries.length)
  if (options.lat) {
    writeEntry(0x0001, 2, 2, 0, options.latRef ?? 'S')
    writeEntry(0x0002, 5, 3, gpsEntries[1]!.value)
    writeEntry(0x0003, 2, 2, 0, options.lngRef ?? 'W')
    writeEntry(0x0004, 5, 3, gpsEntries[3]!.value)
  }
  w32(0)

  // Exif IFD.
  w16(options.taken ? 1 : 0)
  if (options.taken) writeEntry(0x9003, 2, options.taken.length + 1, takenOffset)
  w32(0)

  body.push(...extra)

  const exifHeader = [0x45, 0x78, 0x69, 0x66, 0, 0] // "Exif\0\0"
  const app1Length = 2 + exifHeader.length + body.length
  const bytes = [
    0xff, 0xd8,
    0xff, 0xe1, (app1Length >> 8) & 0xff, app1Length & 0xff,
    ...exifHeader,
    ...body,
    0xff, 0xd9,
  ]
  return new Uint8Array(bytes).buffer
}

describe('readExif', () => {
  it('lê coordenada e data em ordem de bytes grande (MM)', () => {
    const buffer = buildJpegWithExif({
      big: true,
      lat: [28, 23, 20],
      latRef: 'S',
      lng: [49, 25, 19],
      lngRef: 'W',
      taken: '2024:03:15 08:42:11',
    })

    const exif = readExif(buffer)

    expect(exif.takenOn).toBe('2024-03-15')
    expect(exif.coordinates!.latitude).toBeCloseTo(-28.3889, 3)
    expect(exif.coordinates!.longitude).toBeCloseTo(-49.4219, 3)
  })

  it('lê a mesma coisa em ordem de bytes pequena (II)', () => {
    const buffer = buildJpegWithExif({
      big: false,
      lat: [28, 23, 20],
      latRef: 'S',
      lng: [49, 25, 19],
      lngRef: 'W',
      taken: '2024:03:15 08:42:11',
    })

    const exif = readExif(buffer)

    expect(exif.takenOn).toBe('2024-03-15')
    expect(exif.coordinates!.latitude).toBeCloseTo(-28.3889, 3)
  })

  it('norte e leste ficam positivos', () => {
    const buffer = buildJpegWithExif({
      big: true,
      lat: [10, 0, 0],
      latRef: 'N',
      lng: [20, 0, 0],
      lngRef: 'E',
    })

    const exif = readExif(buffer)

    expect(exif.coordinates!.latitude).toBeCloseTo(10, 5)
    expect(exif.coordinates!.longitude).toBeCloseTo(20, 5)
  })

  it('data sem coordenada é válida', () => {
    const exif = readExif(buildJpegWithExif({ big: true, taken: '2020:12:31 23:59:59' }))

    expect(exif.takenOn).toBe('2020-12-31')
    expect(exif.coordinates).toBeNull()
  })

  it('JPEG sem EXIF devolve os dois nulos, sem lançar', () => {
    const semExif = new Uint8Array([0xff, 0xd8, 0xff, 0xd9]).buffer

    expect(readExif(semExif)).toEqual({ coordinates: null, takenOn: null })
  })

  it('arquivo truncado ou que não é JPEG devolve nulos, sem lançar', () => {
    expect(readExif(new Uint8Array([1, 2, 3]).buffer)).toEqual({
      coordinates: null,
      takenOn: null,
    })
    expect(readExif(new ArrayBuffer(0))).toEqual({
      coordinates: null,
      takenOn: null,
    })
  })
})
```

- [ ] **Step 2: Rodar para confirmar que falha**

Run: `npx vitest run src/lib/images/exif.test.ts`
Expected: FAIL — `Failed to resolve import "./exif"`

- [ ] **Step 3: Escrever a implementação**

```ts
import type { Coordinates } from '@/domain/geo'

export interface ExifData {
  coordinates: Coordinates | null
  /** Data civil `YYYY-MM-DD`. Nulo quando o arquivo não diz. */
  takenOn: string | null
}

const VAZIO: ExifData = { coordinates: null, takenOn: null }

const TAG_GPS_IFD = 0x8825
const TAG_EXIF_IFD = 0x8769
const TAG_GPS_LAT_REF = 0x0001
const TAG_GPS_LAT = 0x0002
const TAG_GPS_LNG_REF = 0x0003
const TAG_GPS_LNG = 0x0004
const TAG_DATE_TAKEN = 0x9003

/** Graus, minutos e segundos viram grau decimal. */
function toDegrees(dms: number[], ref: string): number | null {
  const [d, m, s] = dms
  if (d === undefined || m === undefined || s === undefined) return null
  const value = d + m / 60 + s / 3600
  // Sul e Oeste são negativos. Sem isto, uma foto de Santa Catarina cairia na
  // Ucrânia.
  return ref === 'S' || ref === 'W' ? -value : value
}

/** `'2024:03:15 08:42:11'` → `'2024-03-15'`. Nulo se não casar. */
function toCivilDate(exifDate: string): string | null {
  const match = /^(\d{4}):(\d{2}):(\d{2})/.exec(exifDate)
  if (!match) return null
  return `${match[1]}-${match[2]}-${match[3]}`
}

/**
 * Lê GPS e data de um JPEG. Quatro tags, nada mais.
 *
 * NUNCA lança: arquivo sem EXIF, truncado, corrompido ou que nem é JPEG devolve
 * os dois nulos. Uma foto sem metadado é caso comum, não erro — e nulo é
 * exatamente o que o domínio espera para "não sabemos".
 *
 * Escrito à mão em vez de trazer um pacote: são quatro tags, e o ADR 0001 prefere
 * código próprio a um wrapper.
 */
export function readExif(buffer: ArrayBuffer): ExifData {
  try {
    const view = new DataView(buffer)
    if (view.byteLength < 4) return VAZIO
    if (view.getUint16(0) !== 0xffd8) return VAZIO // não é JPEG

    // Procura o segmento APP1 que começa com "Exif\0\0".
    let offset = 2
    let tiff = -1
    while (offset + 4 <= view.byteLength) {
      const marker = view.getUint16(offset)
      if ((marker & 0xff00) !== 0xff00) break
      const size = view.getUint16(offset + 2)
      if (marker === 0xffe1 && offset + 10 <= view.byteLength) {
        if (view.getUint32(offset + 4) === 0x45786966) {
          tiff = offset + 10
          break
        }
      }
      offset += 2 + size
    }
    if (tiff < 0 || tiff + 8 > view.byteLength) return VAZIO

    // `II` é little-endian, `MM` é big-endian. Os dois existem no mundo real.
    const little = view.getUint16(tiff) === 0x4949
    const u16 = (at: number) => view.getUint16(at, little)
    const u32 = (at: number) => view.getUint32(at, little)

    if (u16(tiff + 2) !== 42) return VAZIO

    const readIfd = (start: number): Map<number, { type: number; count: number; at: number }> => {
      const out = new Map<number, { type: number; count: number; at: number }>()
      if (start + 2 > view.byteLength) return out
      const count = u16(start)
      for (let i = 0; i < count; i += 1) {
        const entry = start + 2 + i * 12
        if (entry + 12 > view.byteLength) break
        out.set(u16(entry), {
          type: u16(entry + 2),
          count: u32(entry + 4),
          at: entry + 8,
        })
      }
      return out
    }

    const readAscii = (e: { count: number; at: number }): string => {
      // Até 4 bytes cabem no próprio campo; acima disso ele guarda um offset.
      const start = e.count <= 4 ? e.at : tiff + u32(e.at)
      let out = ''
      for (let i = 0; i < e.count && start + i < view.byteLength; i += 1) {
        const code = view.getUint8(start + i)
        if (code === 0) break
        out += String.fromCharCode(code)
      }
      return out
    }

    const readRationals = (e: { count: number; at: number }): number[] => {
      const start = tiff + u32(e.at)
      const out: number[] = []
      for (let i = 0; i < e.count; i += 1) {
        const at = start + i * 8
        if (at + 8 > view.byteLength) break
        const den = u32(at + 4)
        out.push(den === 0 ? 0 : u32(at) / den)
      }
      return out
    }

    const ifd0 = readIfd(tiff + u32(tiff + 4))

    let takenOn: string | null = null
    const exifPointer = ifd0.get(TAG_EXIF_IFD)
    if (exifPointer) {
      const exifIfd = readIfd(tiff + u32(exifPointer.at))
      const taken = exifIfd.get(TAG_DATE_TAKEN)
      if (taken) takenOn = toCivilDate(readAscii(taken))
    }

    let coordinates: Coordinates | null = null
    const gpsPointer = ifd0.get(TAG_GPS_IFD)
    if (gpsPointer) {
      const gps = readIfd(tiff + u32(gpsPointer.at))
      const lat = gps.get(TAG_GPS_LAT)
      const lng = gps.get(TAG_GPS_LNG)
      const latRef = gps.get(TAG_GPS_LAT_REF)
      const lngRef = gps.get(TAG_GPS_LNG_REF)

      if (lat && lng && latRef && lngRef) {
        const latitude = toDegrees(readRationals(lat), readAscii(latRef))
        const longitude = toDegrees(readRationals(lng), readAscii(lngRef))
        if (
          latitude !== null &&
          longitude !== null &&
          Math.abs(latitude) <= 90 &&
          Math.abs(longitude) <= 180
        ) {
          coordinates = { latitude, longitude }
        }
      }
    }

    return { coordinates, takenOn }
  } catch {
    // Binário malformado não é falha da aplicação: é uma foto sem metadado.
    return VAZIO
  }
}
```

- [ ] **Step 4: Rodar para confirmar que passa**

Run: `npx vitest run src/lib/images/exif.test.ts`
Expected: PASS — 6 testes.

Se algum falhar, o suspeito número um é o cálculo de offsets no **construtor do teste**, não no leitor. Imprima o buffer em hexadecimal e confira contra o layout descrito nos comentários antes de mexer em `exif.ts`.

- [ ] **Step 5: Lint, typecheck, CHANGELOG e commit**

Sob `### Adicionado`:

```markdown
- Uma fotografia com informação de GPS se posiciona sozinha, e a data vem do dia
  em que ela foi tirada — não do dia em que você a subiu
```

```bash
git add src/lib/images/exif.ts src/lib/images/exif.test.ts CHANGELOG.md
git commit -m "feat: leitor de EXIF proprio, quatro tags e nada mais"
```

---

### Task 5: Redimensionamento e caminho de armazenamento

**Files:**
- Create: `src/lib/images/resize.ts`
- Create: `src/lib/images/resize.test.ts`
- Create: `src/lib/images/storage-path.ts`
- Create: `src/lib/images/storage-path.test.ts`

**Interfaces:**
- Produces: `MAX_EDGE_PX`, `targetDimensions(width: number, height: number): { width: number; height: number }`, `resizeToJpeg(file: File): Promise<{ blob: Blob; width: number; height: number }>`, `buildStoragePath(userId: string, placeId: string, uuid: string): string`.

- [ ] **Step 1: Escrever os testes que falham**

`src/lib/images/resize.test.ts` — **só `targetDimensions`**. O `canvas` não existe no ambiente de teste do Vitest, então a casca que desenha fica sem teste unitário e é coberta pela verificação visual da Task 9. Extrair a conta pura é o que torna a parte decidível testável.

```ts
import { describe, expect, it } from 'vitest'
import { MAX_EDGE_PX, targetDimensions } from './resize'

describe('targetDimensions', () => {
  it('encolhe paisagem pelo lado maior, preservando a proporção', () => {
    const r = targetDimensions(4000, 3000)

    expect(r.width).toBe(MAX_EDGE_PX)
    expect(r.height).toBe(Math.round((MAX_EDGE_PX * 3000) / 4000))
  })

  it('encolhe retrato pelo lado maior', () => {
    const r = targetDimensions(3000, 4000)

    expect(r.height).toBe(MAX_EDGE_PX)
    expect(r.width).toBe(Math.round((MAX_EDGE_PX * 3000) / 4000))
  })

  it('NÃO amplia imagem menor que o limite', () => {
    // Ampliar inventaria pixels e engordaria o arquivo sem ganhar nada.
    expect(targetDimensions(800, 600)).toEqual({ width: 800, height: 600 })
  })

  it('imagem exatamente no limite fica intacta', () => {
    expect(targetDimensions(MAX_EDGE_PX, 100)).toEqual({
      width: MAX_EDGE_PX,
      height: 100,
    })
  })

  it('nunca devolve zero', () => {
    // Uma faixa muito estreita arredondaria para zero, e um canvas de altura
    // zero não desenha nada.
    expect(targetDimensions(10000, 1).height).toBeGreaterThanOrEqual(1)
  })
})
```

`src/lib/images/storage-path.test.ts`:

```ts
import { describe, expect, it } from 'vitest'
import { buildStoragePath } from './storage-path'

describe('buildStoragePath', () => {
  it('põe o usuário no primeiro segmento', () => {
    // A política de RLS do Storage compara `(storage.foldername(name))[1]` com
    // `auth.uid()`. Mudar esta ordem quebra a autorização em silêncio.
    const path = buildStoragePath('u-1', 'p-1', 'abc')

    expect(path.split('/')[0]).toBe('u-1')
    expect(path).toBe('u-1/p-1/abc.jpg')
  })

  it('sempre termina em .jpg, porque o canvas reencoda para JPEG', () => {
    expect(buildStoragePath('u', 'p', 'x').endsWith('.jpg')).toBe(true)
  })
})
```

- [ ] **Step 2: Rodar para confirmar que falham**

Run: `npx vitest run src/lib/images/resize.test.ts src/lib/images/storage-path.test.ts`
Expected: FAIL — imports não resolvem.

- [ ] **Step 3: Escrever as implementações**

`src/lib/images/storage-path.ts`:

```ts
/**
 * `{userId}/{placeId}/{uuid}.jpg`.
 *
 * O usuário PRECISA ser o primeiro segmento: a política de RLS do Storage
 * compara `(storage.foldername(name))[1]` com `auth.uid()`. Mudar a ordem
 * quebraria a autorização sem nenhum erro aparecer.
 */
export function buildStoragePath(
  userId: string,
  placeId: string,
  uuid: string,
): string {
  return `${userId}/${placeId}/${uuid}.jpg`
}
```

`src/lib/images/resize.ts`:

```ts
/** Lado maior depois de encolher. */
export const MAX_EDGE_PX = 2000

/** Qualidade do JPEG reencodado. 0.82 é o joelho da curva tamanho/artefato. */
const JPEG_QUALITY = 0.82

/**
 * Dimensões de destino, preservando proporção e **sem ampliar**.
 *
 * Função pura separada da casca que desenha: o `canvas` não existe no ambiente
 * de teste, e é esta conta — não o desenho — que tem regra para errar.
 */
export function targetDimensions(
  width: number,
  height: number,
): { width: number; height: number } {
  const longest = Math.max(width, height)
  if (longest <= MAX_EDGE_PX) return { width, height }

  const scale = MAX_EDGE_PX / longest
  return {
    width: Math.max(1, Math.round(width * scale)),
    height: Math.max(1, Math.round(height * scale)),
  }
}

/**
 * Encolhe e reencoda para JPEG. **Só funciona no navegador.**
 *
 * Reencodar DESTRÓI o EXIF — por isso quem chama precisa ter lido o EXIF do
 * arquivo original ANTES. A ordem não é preferência, é obrigação.
 */
export async function resizeToJpeg(
  file: File,
): Promise<{ blob: Blob; width: number; height: number }> {
  const bitmap = await createImageBitmap(file)
  const target = targetDimensions(bitmap.width, bitmap.height)

  const canvas = document.createElement('canvas')
  canvas.width = target.width
  canvas.height = target.height

  const context = canvas.getContext('2d')
  if (!context) throw new Error('canvas indisponível')
  context.drawImage(bitmap, 0, 0, target.width, target.height)
  bitmap.close()

  const blob = await new Promise<Blob | null>((resolve) =>
    canvas.toBlob(resolve, 'image/jpeg', JPEG_QUALITY),
  )
  if (!blob) throw new Error('não foi possível gerar a imagem')

  return { blob, width: target.width, height: target.height }
}
```

- [ ] **Step 4: Rodar para confirmar que passam**

Run: `npx vitest run src/lib/images/resize.test.ts src/lib/images/storage-path.test.ts`
Expected: PASS — 7 testes.

- [ ] **Step 5: Lint, typecheck, CHANGELOG e commit**

Sob `### Adicionado`:

```markdown
- Fotografias são reduzidas no seu próprio aparelho antes de subir, o que cabe
  cerca de dez vezes mais delas no armazenamento sem diferença visível na tela
```

```bash
git add src/lib/images CHANGELOG.md
git commit -m "feat: encolhe a foto antes de subir, sem ampliar o que ja e pequeno"
```

---

### Task 6: Cliente de navegador e ADR 0014

**Files:**
- Create: `src/lib/supabase/browser.ts`
- Create: `docs/decisions/0014-cliente-supabase-no-navegador.md`

**Interfaces:**
- Consumes: `readSupabaseConfig` de `src/lib/supabase/config.ts`.
- Produces: `getBrowserSupabaseClient(): SupabaseClient | null`.

- [ ] **Step 1: Escrever o cliente**

```ts
'use client'

import { createBrowserClient } from '@supabase/ssr'
import type { SupabaseClient } from '@supabase/supabase-js'
import { readSupabaseConfig } from './config'

let cached: SupabaseClient | null = null

/**
 * Primeiro cliente Supabase de navegador do projeto. Ver ADR 0014.
 *
 * **Existe por um motivo só: subir arquivo direto para o Storage.** Server
 * Actions do Next têm limite de corpo de 1 MB por padrão, e passar binário por
 * lá é frágil por construção.
 *
 * NÃO use para ler nem escrever tabela. Toda tabela continua atrás de Server
 * Action — a restrição está no ADR 0014 e existe para que esta porta não vire
 * um caminho paralelo ao resto da aplicação.
 *
 * `null` quando falta configuração, como o resto do projeto trata Supabase
 * ausente.
 */
export function getBrowserSupabaseClient(): SupabaseClient | null {
  if (cached) return cached

  const config = readSupabaseConfig({
    NEXT_PUBLIC_SUPABASE_URL: process.env.NEXT_PUBLIC_SUPABASE_URL,
    NEXT_PUBLIC_SUPABASE_ANON_KEY: process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY,
  })
  if (!config) return null

  cached = createBrowserClient(config.url, config.anonKey)
  return cached
}
```

- [ ] **Step 2: Escrever o ADR**

`docs/decisions/0014-cliente-supabase-no-navegador.md`:

```markdown
# 0014 — Cliente Supabase no navegador, só para o Storage

**Não altera** o [ADR 0008](./0008-rls-como-fronteira-de-autorizacao.md): a RLS
continua sendo a única fronteira de autorização. **Restringe** o novo cliente a
um uso só.

## Contexto

Até aqui, **toda** escrita do Rastro passava por Server Action. Não existia
cliente Supabase de navegador no projeto.

Fotos mudam isso por um limite técnico: Server Actions do Next têm limite de
corpo de **1 MB** por padrão. Uma foto reduzida cabe por pouco, e qualquer uma
que o redimensionamento não encolha o bastante quebra. Passar binário por Server
Action é frágil por construção.

## Decisão

Existe `src/lib/supabase/browser.ts`, e ele é usado **apenas** para
`storage.from('fotos')`.

Qualquer leitura ou escrita de tabela continua atrás de Server Action.

## Motivos

- **O binário não atravessa o Next.** O arquivo vai do navegador ao Storage
  diretamente, e o limite de corpo deixa de existir.
- **A autorização continua no banco.** Quem decide é a política de RLS sobre
  `storage.objects`, que prende cada usuário à própria pasta. Nenhuma regra de
  acesso migra para código de aplicação.
- **Não amplia o que já é público.** A chave `anon` já vai ao navegador por
  construção — é `NEXT_PUBLIC_`. O que muda é existir uma instância com sessão.

## Consequências

- **Passa a existir no navegador um cliente capaz de fazer tudo que a RLS
  permitir.** Isso não é um furo — é exatamente o modelo do Supabase, e o ADR
  0008 já apostava nele. Mas é superfície nova, e por isso a restrição de uso
  acima é parte da decisão, não uma recomendação.
- **Se algum dia uma tabela precisar ser lida do navegador**, isso muda por
  decisão registrada, não por conveniência de um commit.

## Gatilho de revisão

Se aparecer necessidade de leitura em tempo real (subscription do Supabase, por
exemplo), a restrição a Storage precisa ser reaberta — e aí a conversa é sobre
qual tabela, com qual política, e não sobre o cliente em si.
```

- [ ] **Step 3: Confirmar que nenhuma dependência entrou**

Run: `git diff --stat package.json package-lock.json`
Expected: sem saída. `@supabase/ssr` já é dependência do projeto.

- [ ] **Step 4: Lint, typecheck, CHANGELOG e commit**

Sob `### Adicionado`:

```markdown
- A fotografia sobe do seu aparelho direto para o armazenamento, sem passar pelo
  servidor do aplicativo — o que remove o limite de tamanho que existiria no
  caminho
```

```bash
git add src/lib/supabase/browser.ts docs/decisions/0014-cliente-supabase-no-navegador.md CHANGELOG.md
git commit -m "feat: cliente de navegador, restrito ao Storage por ADR"
```

---

### Task 7: Repositório de fotos

**Files:**
- Create: `src/lib/data/photo-repository.ts`
- Create: `src/lib/data/supabase/photo-row.ts`
- Create: `src/lib/data/supabase/photo-row.test.ts`
- Create: `src/lib/data/supabase/supabase-photo-repository.ts`
- Modify: `src/lib/data/index.ts`

**Interfaces:**
- Consumes: `Photo`, `NewPhoto` de `@/domain/photo`.
- Produces: `PhotoWithUrl extends Photo { url: string }`, `PhotoRepository { listByPlace(placeId): Promise<PhotoWithUrl[]>; addPhoto(input: NewPhoto): Promise<Photo>; deletePhoto(id: string): Promise<{ storagePath: string }> }`, `PHOTO_SELECT`, `PhotoRow`, `toPhotoFromRow(row: PhotoRow): Photo`, `createSupabasePhotoRepository(supabase, userId)`, `getPhotoRepository()`.

- [ ] **Step 1: Escrever o teste de tradução**

```ts
import { describe, expect, it } from 'vitest'
import { toPhotoFromRow } from './photo-row'
import type { PhotoRow } from './photo-row'

function row(overrides: Partial<PhotoRow> = {}): PhotoRow {
  return {
    id: 'f1',
    place_id: 'p1',
    trip_id: null,
    storage_path: 'u1/p1/abc.jpg',
    width: 2000,
    height: 1500,
    latitude: null,
    longitude: null,
    taken_on: null,
    caption: null,
    sort_index: 0,
    ...overrides,
  }
}

describe('toPhotoFromRow', () => {
  it('sem coordenada no banco, devolve null em vez de zero-zero', () => {
    // Zero-zero é um ponto no golfo da Guiné. Fingir coordenada colocaria a
    // foto no Atlântico.
    expect(toPhotoFromRow(row()).coordinates).toBeNull()
  })

  it('monta a coordenada quando as duas colunas existem', () => {
    const photo = toPhotoFromRow(row({ latitude: -28.38, longitude: -49.42 }))

    expect(photo.coordinates).toEqual({ latitude: -28.38, longitude: -49.42 })
  })

  it('data ausente continua nula, e NUNCA vira a data de hoje', () => {
    // Regra do produto: nulo é "não sabemos quando foi tirada". Preencher com
    // hoje seria inventar memória.
    expect(toPhotoFromRow(row({ taken_on: null })).takenOn).toBeNull()
  })

  it('preserva a data quando ela existe', () => {
    expect(toPhotoFromRow(row({ taken_on: '2024-03-15' })).takenOn).toBe(
      '2024-03-15',
    )
  })
})
```

- [ ] **Step 2: Rodar para confirmar que falha**

Run: `npx vitest run src/lib/data/supabase/photo-row.test.ts`
Expected: FAIL — import não resolve.

- [ ] **Step 3: Escrever `photo-row.ts`**

```ts
import type { Photo } from '@/domain/photo'

export const PHOTO_SELECT = `
  id, place_id, trip_id, storage_path, width, height,
  latitude, longitude, taken_on, caption, sort_index
`

export interface PhotoRow {
  id: string
  place_id: string
  trip_id: string | null
  storage_path: string
  width: number
  height: number
  latitude: number | null
  longitude: number | null
  taken_on: string | null
  caption: string | null
  sort_index: number
}

export function toPhotoFromRow(row: PhotoRow): Photo {
  return {
    id: row.id,
    placeId: row.place_id,
    tripId: row.trip_id,
    storagePath: row.storage_path,
    width: row.width,
    height: row.height,
    // `null` explícito, nunca zero-zero: zero-zero é um ponto no golfo da Guiné.
    coordinates:
      row.latitude !== null && row.longitude !== null
        ? { latitude: row.latitude, longitude: row.longitude }
        : null,
    takenOn: row.taken_on,
    caption: row.caption,
    sortIndex: row.sort_index,
  }
}
```

- [ ] **Step 4: Escrever o contrato e o adapter**

`src/lib/data/photo-repository.ts`:

```ts
import type { NewPhoto, Photo } from '@/domain/photo'

/** Foto com a URL assinada pronta para o `<img>`. */
export interface PhotoWithUrl extends Photo {
  url: string
}

/**
 * Nenhum método recebe `userId` para filtrar: a política `photos_own` filtra por
 * `auth.uid()`. Ver ADR 0008.
 */
export interface PhotoRepository {
  listByPlace(placeId: string): Promise<PhotoWithUrl[]>
  addPhoto(input: NewPhoto): Promise<Photo>
  /** Devolve o caminho para que o chamador apague o objeto no Storage. */
  deletePhoto(id: string): Promise<{ storagePath: string }>
}
```

`src/lib/data/supabase/supabase-photo-repository.ts`:

```ts
import type { SupabaseClient } from '@supabase/supabase-js'
import type { PhotoRepository } from '../photo-repository'
import { PHOTO_SELECT, toPhotoFromRow, type PhotoRow } from './photo-row'

/** Uma hora é bem mais que o tempo de olhar um painel, e expira no mesmo dia. */
const SIGNED_URL_TTL_SECONDS = 3600

export function createSupabasePhotoRepository(
  supabase: SupabaseClient,
  userId: string,
): PhotoRepository {
  return {
    async listByPlace(placeId) {
      const { data, error } = await supabase
        .from('photos')
        .select(PHOTO_SELECT)
        .eq('place_id', placeId)
        // Mais recente primeiro, com data desconhecida por último: foto sem data
        // não é foto de hoje, e não deve encabeçar a galeria.
        .order('taken_on', { ascending: false, nullsFirst: false })
        .order('sort_index', { ascending: true })

      if (error) throw new Error(error.message)
      const rows = data as unknown as PhotoRow[]
      if (rows.length === 0) return []

      // UMA chamada para a galeria inteira. Assinar uma por foto multiplicaria
      // ida e volta por nada.
      const { data: signed, error: signError } = await supabase.storage
        .from('fotos')
        .createSignedUrls(
          rows.map((row) => row.storage_path),
          SIGNED_URL_TTL_SECONDS,
        )

      if (signError) throw new Error(signError.message)

      const urlByPath = new Map(
        (signed ?? [])
          .filter((item) => item.signedUrl && item.path)
          .map((item) => [item.path as string, item.signedUrl]),
      )

      return rows
        .map((row) => ({
          ...toPhotoFromRow(row),
          url: urlByPath.get(row.storage_path) ?? '',
        }))
        // Sem URL a imagem não desenha; melhor omitir que mostrar quadro
        // quebrado.
        .filter((photo) => photo.url !== '')
    },

    async addPhoto(input) {
      // `sort_index` é a ordem de upload dentro do lugar. Lido antes da
      // inserção; a corrida entre os dois é aceita, como no slug de lugares:
      // um único usuário não sobe duas fotos no mesmo instante.
      const { data: last } = await supabase
        .from('photos')
        .select('sort_index')
        .eq('place_id', input.placeId)
        .order('sort_index', { ascending: false })
        .limit(1)
        .maybeSingle()

      const nextIndex = ((last as { sort_index: number } | null)?.sort_index ?? -1) + 1

      const { data, error } = await supabase
        .from('photos')
        .insert({
          user_id: userId,
          place_id: input.placeId,
          trip_id: null,
          storage_path: input.storagePath,
          width: input.width,
          height: input.height,
          latitude: input.coordinates?.latitude ?? null,
          longitude: input.coordinates?.longitude ?? null,
          taken_on: input.takenOn,
          caption: input.caption?.trim() || null,
          sort_index: nextIndex,
        })
        .select(PHOTO_SELECT)
        .single()

      if (error) throw new Error(error.message)
      return toPhotoFromRow(data as unknown as PhotoRow)
    },

    async deletePhoto(id) {
      // Apaga a LINHA primeiro: arquivo órfão é desperdício, mas linha apontando
      // para arquivo inexistente é foto quebrada na tela.
      const { data, error } = await supabase
        .from('photos')
        .delete()
        .eq('id', id)
        .select('storage_path')
        .single()

      if (error) throw new Error(error.message)
      return { storagePath: (data as { storage_path: string }).storage_path }
    },
  }
}
```

- [ ] **Step 5: Ligar no `index.ts`**

Acrescente junto dos outros imports e funções de `src/lib/data/index.ts`:

```ts
import { createSupabasePhotoRepository } from './supabase/supabase-photo-repository'
import type { PhotoRepository } from './photo-repository'
```

```ts
export async function getPhotoRepository(): Promise<PhotoRepository> {
  const { supabase, userId } = await sessionContext()
  return createSupabasePhotoRepository(supabase, userId)
}
```

E acrescente `PhotoRepository` à lista do `export type { … }`.

- [ ] **Step 6: Rodar tudo e confirmar que nenhum repositório filtra por `user_id`**

Run: `npx vitest run src/lib/data/supabase/photo-row.test.ts && npm run lint && npm run typecheck`

Busque `eq('user_id'` em `src/lib/data`.
Expected: nenhum resultado além de comentários.

- [ ] **Step 7: CHANGELOG e commit**

Sob `### Adicionado`:

```markdown
- A galeria de um lugar carrega todas as fotos com um único pedido de acesso, em
  vez de um por imagem
```

```bash
git add src/lib/data/photo-repository.ts src/lib/data/supabase/photo-row.ts src/lib/data/supabase/photo-row.test.ts src/lib/data/supabase/supabase-photo-repository.ts src/lib/data/index.ts CHANGELOG.md
git commit -m "feat: repositorio de fotos, com URL assinada em lote"
```

---

### Task 8: Server Actions

**Files:**
- Create: `src/app/actions/photo-actions.ts`

**Interfaces:**
- Consumes: `validateNewPhoto`, `PHOTO_VALIDATION_MESSAGES`, `NewPhoto` de `@/domain/photo`; `getPhotoRepository` de `@/lib/data`; `ActionResult` de `./result`.
- Produces: `addPhotoAction(input: NewPhoto): Promise<ActionResult>`, `deletePhotoAction(id: string): Promise<ActionResult & { storagePath?: string }>`.

- [ ] **Step 1: Escrever as actions**

```ts
'use server'

import { revalidatePath } from 'next/cache'
import {
  PHOTO_VALIDATION_MESSAGES,
  validateNewPhoto,
  type NewPhoto,
} from '@/domain/photo'
import { getPhotoRepository } from '@/lib/data'
import type { ActionResult } from './result'

/**
 * Grava a linha de uma foto que JÁ subiu para o Storage.
 *
 * O binário não passa por aqui: ele foi direto do navegador para o bucket. Esta
 * action só registra o que aconteceu — ver ADR 0014.
 */
export async function addPhotoAction(input: NewPhoto): Promise<ActionResult> {
  const [firstError] = validateNewPhoto(input)
  if (firstError) {
    return { ok: false, message: PHOTO_VALIDATION_MESSAGES[firstError] }
  }

  try {
    const repository = await getPhotoRepository()
    await repository.addPhoto(input)
  } catch {
    return { ok: false, message: 'Não foi possível salvar a fotografia.' }
  }

  revalidatePath('/', 'layout')
  return { ok: true }
}

/**
 * Apaga a linha e devolve o caminho, para o cliente remover o objeto.
 *
 * A linha sai primeiro de propósito: arquivo órfão é desperdício, mas linha
 * apontando para arquivo inexistente é foto quebrada na tela.
 */
export async function deletePhotoAction(
  id: string,
): Promise<ActionResult & { storagePath?: string }> {
  try {
    const repository = await getPhotoRepository()
    const { storagePath } = await repository.deletePhoto(id)
    revalidatePath('/', 'layout')
    return { ok: true, storagePath }
  } catch {
    return { ok: false, message: 'Não foi possível apagar a fotografia.' }
  }
}
```

- [ ] **Step 2: Lint, typecheck, CHANGELOG e commit**

Run: `npm run lint && npm run typecheck && npm test -- --run`

Sob `### Adicionado`:

```markdown
- Apagar uma fotografia remove o registro e o arquivo, nessa ordem — uma imagem
  quebrada na tela seria pior que um arquivo esquecido
```

```bash
git add src/app/actions/photo-actions.ts CHANGELOG.md
git commit -m "feat: acoes de foto, com a linha apagada antes do arquivo"
```

---

### Task 9: A galeria no painel do lugar

**Files:**
- Create: `src/components/explore/PlacePhotos.tsx`
- Modify: `src/components/explore/PlacePanel.tsx`
- Modify: `src/components/explore/PlaceActions.tsx` (remover "Em breve: Fotos da viagem")

**Interfaces:**
- Consumes: tudo das tarefas 3 a 8.
- Produces: componente `PlacePhotos`.

- [ ] **Step 1: Escrever o componente**

`PlacePhotos.tsx` é **cliente** (precisa de `<input type="file">`, canvas e o cliente de navegador). Recebe as fotos já com URL assinada, vindas do servidor.

Estrutura obrigatória:

- Cabeçalho com `instrument-label`: `"N fotografias"` ou `"Sem fotografias"` — substitui a linha que hoje vive no `PlacePanel`.
- `<input type="file" accept="image/jpeg,image/png,image/webp" multiple>` dentro de um `<label>` estilizado como `Button` `outline` `sm`. **Sem glifo** (ADR 0011).
- Para cada arquivo escolhido, nesta ordem exata:
  1. `isAcceptedImageType(file.type)` — se falso, `InlineMessage` `tone="warn"` dizendo os formatos aceitos, e **para aqui**
  2. `readExif(await file.arrayBuffer())` — **antes** de encolher
  3. `resizeToJpeg(file)`
  4. `getBrowserSupabaseClient()!.storage.from('fotos').upload(path, blob)` com `buildStoragePath(userId, placeId, crypto.randomUUID())`
  5. `addPhotoAction({ ... })`
  6. **se a action recusar, apagar o objeto recém-subido** antes de mostrar o erro
- Grade: `grid grid-cols-3 gap-1`, cada célula com `aspect-ratio` vindo de `width/height` **da própria foto**, `<img>` com `object-cover`, `loading="lazy"`.
- Sob cada foto aberta: legenda quando houver, e a data em `.instrument-value` — ou `"data desconhecida"` quando `takenOn` for nulo.
- Apagar: dois passos, como em `EditPlaceView` e `TripDeleteAction` — `InlineMessage` `tone="warn"` nomeando o custo, e `Button` `variant="danger"`.

**O `userId` sai do próprio cliente de navegador**, não de uma prop:

```ts
const { data } = await client.auth.getUser()
const userId = data.user?.id
if (!userId) return // sem sessão o middleware já teria redirecionado
```

Passá-lo do servidor obrigaria o componente de página a descobrir a identidade só
para repassá-la — e o cliente de navegador já tem a sessão, que é justamente o
que a política de Storage vai conferir.

**Antes de escrever, leia `src/components/explore/PlaceVisits.tsx`** — ele já resolve exatamente esta forma (lista com remover, `useTransition`, `InlineMessage`) e a nova peça deve parecer irmã dele.

- [ ] **Step 2: Ligar no `PlacePanel`**

Substitua o bloco de `place.photoCount` (linhas ~70-78) por `<PlacePhotos … />`, mantendo o bloco de `coverImageUrl` como está. Busque as fotos no componente servidor que renderiza o painel, via `getPhotoRepository().listByPlace(place.id)`.

Em `PlaceActions.tsx`, remova o bloco `"Em breve / Fotos da viagem"` inteiro — ele deixou de ser verdade.

- [ ] **Step 3: Lint e typecheck**

Run: `npm run lint && npm run typecheck`
Expected: limpos.

- [ ] **Step 4: VERIFICAÇÃO VISUAL — obrigatória**

Suba o servidor, entre, abra um lugar e **suba uma foto de verdade**. Capture em 1512 e 390 e **olhe as imagens**.

Confirme, olhando:
- a grade aparece, as miniaturas carregam, nada pula ao carregar
- a legenda e a data aparecem, e foto sem EXIF mostra `"data desconhecida"` — **não** a data de hoje
- um arquivo `.heic` é recusado com mensagem, antes de qualquer upload

- [ ] **Step 5: A PROVA DO CICLO — o `photoDot` tem que acender**

Depois do upload, volte ao mapa em `/`.

Expected: o pin daquele lugar ganha **um ponto de osso no canto superior direito**. Ele já existe em `layers.ts` (`PLACE_LAYERS.photoDot`, raio 1,7, deslocamento `[8, -8]`) e está apagado desde sempre porque `photo_count` era zero.

**Se não acender, o trigger da Task 1 não funcionou** — confira no SQL Editor:

```sql
select photo_count from place_user_states where place_id = '<id>';
```

- [ ] **Step 6: Rodar a prova de RLS de novo**

Run: `node scripts/verificar-storage-rls.mjs`
Expected: três `OK:`. Agora com objetos reais no bucket, o teste vale mais do que valia vazio.

- [ ] **Step 7: CHANGELOG e commit**

Sob `### Adicionado`:

```markdown
- **Fotografias num lugar.** Suba do painel do lugar e elas aparecem ali mesmo, na
  ordem em que foram tiradas. A foto que traz data no arquivo mostra a data; a que
  não traz diz "data desconhecida", em vez de fingir que foi tirada hoje. O pin
  no mapa passa a marcar quais lugares têm fotografia
```

Sob `### Removido`:

```markdown
- O aviso de "Em breve: fotos da viagem" no painel do lugar
```

```bash
git add src/components/explore/PlacePhotos.tsx src/components/explore/PlacePanel.tsx src/components/explore/PlaceActions.tsx CHANGELOG.md
git commit -m "feat: galeria de fotos no lugar, e o pin passa a marcar quem tem"
```

---

## Encerramento

- [ ] `npm run lint && npm run typecheck && npm test -- --run` — limpos, ≥ 175 testes
- [ ] `node scripts/verificar-storage-rls.mjs` — três `OK:`
- [ ] `git diff --stat main...HEAD -- package.json package-lock.json` — vazio
- [ ] `docs/ARCHITECTURE.md`: acrescentar `src/lib/images/` à tabela de camadas
- [ ] `docs/DESIGN-SYSTEM.md`: registrar que a grade de fotos reserva proporção em vez de usar skeleton

Commit: `docs: registra a camada de imagens e a grade que reserva proporcao`

---

## Rastreio da spec

| Requisito | Tarefa |
|---|---|
| §3.1 Foto pertence ao lugar, viagem opcional | 1 |
| §4 `place_id` cascade, width/height not null, drop exif | 1 |
| §4 Trigger `refresh_place_photo_count` | 1, provado em 9 |
| §3.2 Bucket privado e políticas | 2 |
| §11 Prova de RLS do Storage | 2, repetida em 9 |
| §4 Tipos e validação do domínio | 3 |
| §3.3 Ler EXIF antes de encolher | 4, 5, ordem imposta em 9 |
| §5 HEIC recusado com motivo | 3 (`isAcceptedImageType`), 9 (mensagem) |
| §3.4 Upload direto do navegador | 6, 9 |
| §6 ADR 0014 restringindo a Storage | 6 |
| §7 URL assinada em lote | 7 |
| §4 `sortIndex` = ordem de upload | 7 |
| §4 `takenOn` nulo é "não sabemos" | 3, 7 (teste), 9 (interface) |
| §9 Apagar linha antes do arquivo | 7, 8 |
| §8 Limpeza do órfão em caso de recusa | 9 |
| §9 Sem `next/image`, sem skeleton, reserva de proporção | 9 |
| §10 Erros no painel | 9 |
| §11 Verificação visual e prova do `photoDot` | 9 |

> **Fraqueza conhecida:** a Task 9 especifica o componente por contrato e por
> referência a `PlaceVisits.tsx`, em vez de trazer o JSX inteiro — mesma escolha
> (e mesmo risco) das telas de Viagens. É onde a revisão entre tarefas importa
> mais. **Leia `PlaceVisits.tsx` antes de escrever.**
